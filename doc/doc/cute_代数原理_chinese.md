# CuTe布局代数

## 简介

CuTe布局代数对于理解和应用CUTLASS进行加速计算极其重要。尽管CuTe有关于其布局代数的文档，但如果不首先理解其数学基础，就无法完全理解它。我曾尝试自己为CuTe布局代数创建一些证明，并意识到这是一项巨大的工作。值得庆幸的是，Jay Shah创建了一篇论文《关于CuTe布局代数的注记》，它完善了我本想创建的CuTe布局代数数学基础。

在我的校对过程中，我发现Jay Shah的论文几乎无错误，除了一些非常小的疏忽和拼写错误。然而，它确实跳过了一些细节，没有这些细节，论文有点难以理解。在本文中，基于Jay Shah的论文，我想提供更多关于CuTe布局代数的证明和解释，其中一些在Jay Shah的论文中并未出现。大多数定义和注释将遵循Jay Shah的论文。

本文可以作为Jay Shah论文的补充阅读，但对于理解CuTe布局代数，它也是完全独立的。

## 布局代数预备知识

### 定义 2.1: 布局

一个布局 \(L\) 是一对维度匹配的正整数元组 \(\mathbf{S}\) 和 \(\mathbf{D}\)。我们称 \(\mathbf{S}\) 为形状，\(\mathbf{D}\) 为步长。我们写作 \(L=\mathbf{S}:\mathbf{D}\)。

一个展平的布局意味着形状和步长中没有内部括号。例如，\(L=(5,2,2):(16,80,4)\) 是一个展平的布局，而 \(L=(5,(2,2)):(16,(80,4))\) 则不是。展平布局不会改变布局的语义和操作。

### 定义 2.2: 布局大小、长度和模式

令 \(\alpha\geq 0\) 为整数，且 \(L=\mathbf{S}:\mathbf{D}=(M_{0},M_{1},...,M_{\alpha}):(d_{0},d_{1},...,d_{ \alpha})\) 是一个布局。那么：

*   *\(L\) 的大小是乘积 \(M=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha}\)。
*   *\(L\) 的长度是整数 \(\alpha+1\)。
*   *\(L\) 的一个模式是其中一个条目 \((M_{k}):(d_{k})\)，其中 \(0\leq k\leq\alpha\)。我们可以将其视为长度为 1 的布局。

### 连接

给定两个布局 \(L=\mathbf{S}:\mathbf{D}\) 和 \(L^{\prime}=\mathbf{S}^{\prime}:\mathbf{D}^{\prime}\)，令 \(\mathbf{S}^{\prime\prime}\) 和 \(\mathbf{D}^{\prime\prime}\) 分别是由（展平的）\((\mathbf{S},\mathbf{S}^{\prime})\) 和 \((\mathbf{D},\mathbf{D}^{\prime})\) 给出的形状和步长元组。那么 \(L\) 和 \(L^{\prime}\) 的连接由布局给出

\[(L,L^{\prime})=\mathbf{S}^{\prime\prime}:\mathbf{D}^{\prime\prime}\]

我们说 \((L,L^{\prime})\) 由 \(L\) 和 \(L^{\prime}\) 分解。

归纳地，给定布局 \(L_{0},L_{1},...,L_{N}\)，我们可以形成连接 \((L_{0},L_{1},...,L_{N})\)。反之，给定一个布局 \(L\)，\(L\) 由其模式极大分解。

**同构**

令 \(\mathbf{S}=(M_{0},M_{1},...,M_{\alpha})\) 和 \(\mathbf{D}=(d_{0},d_{1},...,d_{\alpha})\) 分别为 \(L=\mathbf{S}:\mathbf{D}\) 的形状和步长元组。令 \(M=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha}\) 为 \(L\) 的大小，并令 \([0,M)\subset\mathbb{N}\) 是由 \(0,1,2,...,M-1\) 给出的自然数子集。那么我们有一个同构

\[\iota:[0,M)\cong[0,M_{0})\times[0,M_{1})\times...\times[0,M_{\alpha})\]

给定任何 \(x\in[0,M)\)，同构 \(\iota\) 将 \(x\) 映射到元组

\[x\mapsto\left(x\mod{M_{0}},\left\lfloor\frac{x}{M_{0}}\right\rfloor\mod{M_{1}}, ...,\left\lfloor\frac{x}{M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}}\right \rfloor\mod{M_{\alpha}}\right)\]

这个同构映射是双射的。在我们的情况下，给定任何元组 \((x_{0},x_{1},...,x_{\alpha})\in[0,M_{0})\times[0,M_{1})\times...\times[0,M_{ \alpha})\)，同构逆映射将元组映射到整数

\[(x_{0},x_{1},...,x_{\alpha})\mapsto x_{0}+x_{1}\cdot M_{0}+x_{2}\cdot M_{0} \cdot M_{1}+...+x_{\alpha}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\]

很容易验证上述同构映射是有效的，并且证明上述同构映射是双射的（通过反证法）。

人们可以将这个同构想象为一维坐标和多维坐标之间的映射。

**注释：**
此处可以理解为：
\[
(M_{0}, M_{1}, \dots, M_{\alpha}: 1, M_{0}, M_{0}M_{1}, \dots, M_{0}M_{1}\cdots M_{\alpha})
\]

**定义 2.3: 布局函数**

给定一个布局 \(L\)，其布局函数是函数 \(f_{L}:[0,M)\rightarrow\mathbb{N}\)，定义为复合

\[[0,M)\cong[0,M_{0})\times[0,M_{1})\times...\times[0,M_{\alpha})\subset\mathbb{N }^{\times(\alpha+1)}\overset{d_{0},d_{1},...,d_{\alpha}}{\rightarrow}\mathbb{N }^{\times(\alpha+1)}\overset{+}{\rightarrow}\mathbb{N}\]

换句话说，\(f_{L}\) 是由步长决定的多线性函数

\[[0,M_{0})\times[0,M_{1})\times...\times[0,M_{\alpha}) \rightarrow\mathbb{N}\] \[(x_{0},x_{1},...,x_{\alpha})\mapsto x_{0}\cdot d_{0}+x_{1}\cdot d _{1}+...+x_{\alpha}\cdot d_{\alpha}\]

与由形状决定的同构 \(\iota\) 的复合。

计算布局函数 \(f_{L}\) 在点 \(x\in[0,M)\) 处的值可以分解为计算布局函数在多个点处的值之和。这对于方便地计算布局函数在某点的值有时很有用。

给定一个布局 \(L=(M_{0},M_{1},...,M_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\) 和 \(x\in[0,M)\)，

\[x\mapsto(x_{0},x_{1},...,x_{\alpha})\mapsto x_{0}\cdot d_{0}+x_{1}\cdot d_{1}+ ...+x_{\alpha}\cdot d_{\alpha}\]

我们还有

\[x_{0}^{\prime} \mapsto(x_{0},0,0,...,0)\mapsto x_{0}\cdot d_{0}\] \[x_{1}^{\prime} \mapsto(0,x_{1},0,...,0)\mapsto x_{1}\cdot d_{1}\] \[\vdots\] \[x_{\alpha}^{\prime} \mapsto(0,0,0,...,x_{\alpha})\mapsto x_{\alpha}\cdot d_{\alpha}\]

因此，我们有

\[f_{L}(x)=f_{L}(x^{\prime}_{0})+f_{L}(x^{\prime}_{1})+...+f_{L}(x^{\prime}_{\alpha})\]

其中

\[\begin{array}{lll}x^{\prime}_{0}&=x&mod\ M_{0}\\ x^{\prime}_{1}&=\left\lfloor\frac{x}{M_{0}}\right\rfloor&mod\ M_{1}\cdot M_{0} \\ &&\vdots\\ x^{\prime}_{\alpha}&=\left\lfloor\frac{x}{M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha -1}}\right\rfloor&mod\ M_{\alpha}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha- 1}\end{array}\]

例如，给定一个布局 \(L=(3,2):(2,3)\) 和 \(x=5\)，我们有

\[f_{L}(5) =f_{L}(5\ \ \ mod\ 3)+f_{L}\left(\left\lfloor\frac{5}{3}\right \rfloor\ \ \ mod\ 2\cdot 3\right)\] \[=f_{L}(2)+f_{L}(3)\] \[=2\cdot 2+\left\lfloor\frac{3}{3}\right\rfloor\cdot 3\] \[=4+3\] \[=7\]

**布局函数的扩展**

基于布局函数的定义，布局函数 \(f_{L}\) 的扩展是函数 \(\hat{f}_{L}:{\mathbb{N}}\rightarrow{\mathbb{N}}\)，通过在 \(f_{L}\) 的定义中将 \(M_{\alpha}\) 替换为 \(\infty\) 来定义，即复合

\[{\mathbb{N}}\cong[0,M_{0})\times[0,M_{1})\times...\times[0,M_{\alpha-1})\times{ \mathbb{N}}\subset{\mathbb{N}}^{\times(\alpha+1)}\stackrel{{ d_{0}, \cdot,d_{1},\ldots,d_{\alpha}}}{{\rightarrow}}{\mathbb{N}}^{\times(\alpha+1)} \stackrel{{+}}{{\rightarrow}}{\mathbb{N}}\]

其中同构 \(\iota\) 的扩展 \(\hat{\iota}\) 由下式给出

\[x\mapsto\left(x\ \ \ mod\ M_{0},\left\lfloor\frac{x}{M_{0}}\right\rfloor\ \ \ mod\ M_{1},...,\left\lfloor\frac{x}{M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-2}}\right\rfloor\ \ \ mod\ M_{\alpha-1},\left\lfloor\frac{x}{M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}}\right\rfloor\right)\]

同构映射的扩展也是双射的。同构扩展的逆映射也由下式给出

\[(x_{0},x_{1},...,x_{\alpha-1},x_{\alpha})\mapsto x_{0}+x_{1}\cdot M_{0}+x_{2} \cdot M_{0}\cdot M_{1}+...+x_{\alpha}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{ \alpha-1}\]

人们可以想象同构的扩展将形状的最后一个维度定义为“批处理”维度，并且批处理大小可以是无限的。

## 合并

合并简化了布局且不改变布局函数。

**合并规则**

考虑一个只有两个整数模式的布局，\(A=(N_{0},N_{1}):(d_{0},d_{1})\)，我们需要考虑四种情况：

1.  \(N_{1}=1\)。
2.  \(N^{\prime}_{0}=1\)。
3.  \(d_{1}=N_{0}d_{0}\)。
4.  其他任何情况。

在第一种情况下，显然，\(A=(N_{0},1):(d_{0},d_{1})=(N_{0}):(d_{0})\)。这可以进一步展平为 \(A=N_{0}:d_{0}\)。

在第二种情况下，同样显然，\(A=(1,N_{1}):(d_{0},d_{1})=(N_{1}):(d_{1})\)。这可以进一步展平为 \(A=N_{1}:d_{1}\)。

在第三种情况下，我们有 \(A=(N_{0},N_{1}):(d_{0},N_{0}d_{0})=(N_{0}N_{1}):(d_{0})\)。这可以进一步展平为 \(A=N_{0}N_{1}:d_{0}\)。

在第四种情况下，我们什么也做不了，\(A\) 保持不变。

有一种情况经常被误解，即 \(d_{0}=N_{1}d_{1}\)。在这种情况下，我们有 \(A=(N_{0},N_{1}):(N_{1}d_{1},d_{1})\)。乍一看，似乎可以将 \(A\) 合并为 \((N_{0}N_{1}):(d_{1})\)。然而，这是不正确的，因为它改变了布局函数。

考虑一个具有多于两个整数模式的布局，我们可以递归地应用上述规则，每次尝试合并两个相邻的整数模式，直到无法再合并为止。这保证了布局函数保持不变。

证明

令 \(L=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\) 为布局 \(L_{0}\), \(L_{1}\), ..., \(L_{\alpha}\) 连接的布局，其中每个 \(L_{k}=(N_{k}):(d_{k})\)，\(k\in[0,\alpha]\)。给定一个坐标 \(x\mapsto(x_{0},x_{1},...,x_{\alpha})\)，我们得到布局 \(L\) 的布局函数如下：

\[f_{L}(x) =x_{0}d_{0}+x_{1}d_{1}+x_{2}d_{2}+...+x_{\alpha}d_{\alpha}\]

布局 \(L\) 的布局函数就是每个 \(L_{k}\) 的布局函数之和，即，

\[f_{L}(x) =f_{L_{0}}(x_{0})+f_{L_{1}}(x_{1})+...+f_{L_{\alpha}}(x_{\alpha})\] \[=x_{0}d_{0}+x_{1}d_{1}+x_{2}d_{2}+...+x_{\alpha}d_{\alpha}\]

假设两个相邻的整数模式，比如 \(A=(L_{i},L_{i+1})=(N_{i},N_{i+1}):(d_{i},d_{i+1})\)，可以合并为一个新布局 \(A^{\prime}_{i}\)，其形状为 \((N_{i}N_{i+1})\)，且满足第一、第二和第三合并规则。根据合并的定义，我们必须有 \(f_{A^{\prime}}(x)=f_{A}(x)\)。

对于任何 \(x_{i}\in[0,N_{i})\) 和 \(x_{i+1}\in[0,N_{i+1})\)，我们有 \(x^{\prime}=x_{i}+x_{i+1}\cdot N_{i}\)，其中 \(x^{\prime}\in[0,N_{i}N_{i+1})\)。

$$\begin{aligned}
f_{A^{\prime}}(x^{\prime}) &= f_{A}(x^{\prime}) \\
&= x_{i} d_{i} + x_{i + 1} d_{i + 1} \\
&= f_{L_{i}}(x_{i}) + f_{L_{i + 1}}(x_{i + 1}) \\
\end{aligned}$$

给定布局 \(L\) 的任何坐标 \(x\mapsto(x_{0},x_{1},...,x_{i-1},x_{i},x_{i+1},x_{i+2},...,x_{\alpha})\)，将 \(A\) 合并为 \(A^{\prime}\) 后，布局 \(L^{\prime}\)（即合并 \(A\) 为 \(A^{\prime}\) 后的布局）的坐标为 \(x\mapsto(x_{0},x_{1},...,x_{i-1},x^{\prime},x_{i+2},...,x_{\alpha})\)。

这很容易验证，因为坐标同构。

$$\begin{aligned}
x &= x_{0} + x_{1} \cdot N_0 + x_{2} \cdot N_0 N_1 + \ldots + x_{i - 1} \cdot N_0 N_1 \ldots N_{i - 2} + x_{i} \cdot N_0 N_1 \ldots N_{i - 1} + x_{i + 1} \cdot N_0 N_1 \ldots N_{i - 1} N_i + x_{i + 2} \cdot N_0 N_1 \ldots N_{i - 1} N_i N_{i + 1} + \ldots + x_{\alpha} \cdot N_0 N_1 \ldots N_{\alpha - 1} \\
&= x_{0} + x_{1} \cdot N_0 + x_{2} \cdot N_0 N_1 + \ldots + x_{i - 1} \cdot N_0 N_1 \ldots N_{i - 2} + (x_{i} + x_{i + 1} \cdot N_i) \cdot N_0 N_1 \ldots N_{i - 1} + x_{i + 2} \cdot N_0 N_1 \ldots N_{i - 1} N_i N_{i + 1} + \ldots + x_{\alpha} \cdot N_0 N_1 \ldots N_{\alpha - 1} \\
&= x_{0} + x_{1} \cdot N_0 + x_{2} \cdot N_0 N_1 + \ldots + x_{i - 1} \cdot N_0 N_1 \ldots N_{i - 2} + x^{\prime} \cdot N_0 N_1 \ldots N_{i - 1} + x_{i + 2} \cdot N_0 N_1 \ldots N_{i - 1} N_i N_{i + 1} + \ldots + x_{\alpha} \cdot N_0 N_1 \ldots N_{\alpha - 1} \\
\end{aligned}$$
那么我们有
$$\begin{aligned}
f_{L^{\prime}}(x)
&= f_{L_{0}}(x_{0}) + f_{L_{1}}(x_{1}) + \ldots + f_{L_{i - 1}}(x_{i - 1}) + f_{A^{\prime}}(x^{\prime}) + f_{L_{i + 2}}(x_{i + 2}) + \ldots + f_{L_{\alpha}}(x_{\alpha}) \\
&= f_{L_{0}}(x_{0}) + f_{L_{1}}(x_{1}) + \ldots + f_{L_{i - 1}}(x_{i - 1}) + f_{L_{i}}(x_{i}) + f_{L_{i + 1}}(x_{i + 1}) + f_{L_{i + 2}}(x_{i + 2}) + \ldots + f_{L_{\alpha}}(x_{\alpha}) \\
&= f_{L}(x)
\end{aligned}$$
因此，将 \(A\) 合并为 \(A^{\prime}\) 后，布局函数保持不变。

证明完毕。

#### 按模式合并

在某些情况下，当布局中的模式并非完全整数时，如果我们希望保持模式数量不变，可以执行按模式合并。这可以通过在具有多于两个整数模式的布局的合并规则中禁用来自两个不同模式的相邻整数模式的合并来实现。

例如，如果我们有一个具有两个模式的布局 \(A=((N_{0},N_{1},...,N_{\alpha}),(N_{\alpha+1},N_{\alpha+2},...,N_{\beta}):((d_ {0},d_{1},...,d_{\alpha}),(d_{\alpha+1},d_{\alpha+2},...,d_{\beta})))\)，要执行按模式合并，我们可以分别合并整数模式 \((N_{0},N_{1},...,N_{\alpha})\) 和 \((N_{\alpha+1},N_{\alpha+2},...,N_{\beta})\)，直到每个都无法再合并为止，并且即使这两个后续合并的模式可以合并，也不会在它们之间执行进一步的合并。这将得到一个与之前模式数量相同的布局。

#### 合并的含义

合并简化了布局且不改变布局函数。它是一种在保留功能的同时减少布局和相关计算复杂性的有用操作。如果对布局执行非按模式合并，则布局可以被简化，使得每个模式都是一个整数模式，即 \(A=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\)，其中 \(N_{k}\) 是整数，而不是整数元组，并且对于 \(k\in[0,\alpha]\)，\(N_{k}>1\)。

合并的性质非常重要，我们将在本文中提出的大多数证明都假设布局是合并的。这个假设在数学上是可行的，因为合并不会改变布局函数。

## 补集

**定义 2.4: 排序布局**

令 \(A=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\) 为一个布局。如果 \(d_{0}\leq d_{1}\leq...\leq d_{\alpha}\) 且对于每个 \(i<j\)，如果 \(d_{i}=d_{j}\)，则 \(N_{i}\leq N_{j}\)，我们说 \(A\) 是排序的。

请注意，对布局进行排序，或者更一般地说，改变布局模式的顺序，将改变布局的语义和操作。

例如，假设我们有一个布局 \(A=(2,4):(4,1)\) 和一个布局 \(B=(4,2):(1,4)\)。我们可以看到 \(B\) 是 \(A\) 的排序版本。我们可以使用查找表计算 \(A\) 和 \(B\) 的布局函数如下：

\(f_{A}(0) = f_{A}(0,0) = 0 \cdot 4 + 0 \cdot 1 = 0\)

\(f_{A}(1) = f_{A}(1,0) = 1 \cdot 4 + 0 \cdot 1 = 4\)

\(f_{A}(2) = f_{A}(0,1) = 0 \cdot 4 + 1 \cdot 1 = 1\)

\(f_{A}(3) = f_{A}(1,1) = 1 \cdot 4 + 1 \cdot 1 = 5\)

\(f_{A}(4) = f_{A}(0,2) = 0 \cdot 4 + 2 \cdot 1 = 2\)

\(f_{A}(5) = f_{A}(1,2) = 1 \cdot 4 + 2 \cdot 1 = 6\)

\(f_{A}(6) = f_{A}(0,3) = 0 \cdot 4 + 3 \cdot 1 = 3\)

\(f_{A}(7) = f_{A}(1,3) = 1 \cdot 4 + 3 \cdot 1 = 7\)

\(f_{B}(0) = f_{B}(0,0) = 0 \cdot 1 + 0 \cdot 4 = 0\)

\(f_{B}(1) = f_{B}(1,0) = 1 \cdot 1 + 0 \cdot 4 = 1\)

\(f_{B}(2) = f_{B}(2,0) = 2 \cdot 1 + 0 \cdot 4 = 2\)

\(f_{B}(3) = f_{B}(3,0) = 3 \cdot 1 + 0 \cdot 4 = 3\)

\(f_{B}(4) = f_{B}(0,1) = 0 \cdot 1 + 1 \cdot 4 = 4\)

\(f_{B}(5) = f_{B}(1,1) = 1 \cdot 1 + 1 \cdot 4 = 5\)

\(f_{B}(6) = f_{B}(2,1) = 2 \cdot 1 + 1 \cdot 4 = 6\)

\(f_{B}(7) = f_{B}(3,1) = 3 \cdot 1 + 1 \cdot 4 = 7\)

我们可以看到布局 \(B\) 通常被称为列主序布局，而布局 \(A\) 通常被称为行主序布局。它们是完全不同的布局。

更一般地，排序布局就像是列主序布局的“泛化”。

**定义 2.5: 补集的可接纳性**

令 \(A = (N_0, N_1, ..., N_\alpha) : (d_0, d_1, ..., d_\alpha)\) 为一个布局，\(M\) 为一个正整数。如果 \(A\) 没有排序，则将其替换为其排序版本。如果满足以下条件，我们说对 \(\{A, M\}\) 是可接纳的（对于补集，或简称为可接纳）：

- 对于所有 \(1 \leq i \leq \alpha\)，\(N_{i-1} \cdot d_{i-1}\) 整除 \(d_i\)。
- \(N_\alpha \cdot d_\alpha\) 整除 \(M\)。

\(\{A, M\}\) 对于补集可接纳也意味着：

- 对于所有 \(1 \leq i \leq \alpha\)，\(N_{i-1} \cdot d_{i-1} \leq d_i\) 且 \(d_{i-1} \leq d_i\)。
- \(N_\alpha \cdot d_\alpha \leq M\) 且 \(d_\alpha \leq M\)。

**定义 2.6: 补集**

令 \(A = (N_0, N_1, ..., N_\alpha) : (d_0, d_1, ..., d_\alpha)\) 为一个布局，\(M\) 为一个正整数。如果 \(\{A, M\}\) 对于补集是可接纳的，那么如果 \(A\) 没有排序，则将其替换为其排序版本。\(\{A, M\}\) 的补集定义为布局

\[complement(A, M) = (d_0, \frac{d_1}{N_0 d_0}, \frac{d_2}{N_1 d_1}, ..., \frac{d_\alpha}{N_{\alpha-1} d_{\alpha-1}}, \frac{M}{N_\alpha d_\alpha}) : (1, N_0 d_0, N_1 d_1, ..., N_\alpha d_\alpha)\]

注意，\(\{A, M\}\) 的补集的大小，\(\text{size}(complement(A, M))\)，是 $\frac{M}{\text{size}(A)} = \frac{M}{N_{0} \cdot N_{1} \cdot \ldots \cdot N_{\alpha}}$。

根据定义，\(\{A, M\}\) 的补集对 \(A\) 模式的顺序不敏感，因为它总会在取补集之前被排序。

\(\{A, M\}\) 的补集是严格递增的。这可能不是非常明显，所以我们将展示一个证明。

证明

假设 \(B\,=\,\) complement\((A,M)\)，为了证明布局函数 \(f_{B}\)（其定义域是自然数集合）是严格递增的，我们需要证明对于每两个相邻的自然数 \(x\) 和 \(x+1,0\leq x<x+1<\text{size}(B)\)，我们有 \(f_{B}(x)<f_{B}(x+1)\)。

由于同构，假设 \(x\) 的映射如下：

\[x\ \mapsto(x_{0},x_{1},...,x_{\alpha},x_{\alpha+1})\]

根据布局函数 \(f_{B}\) 的定义，我们有

\[f_{B}(x)\ \ =x_{0}+x_{1}\cdot N_{0}d_{0}+x_{2}\cdot N_{1}d_{1}+...+x_{\alpha} \cdot N_{\alpha-1}d_{\alpha-1}+x_{\alpha+1}\cdot N_{\alpha}d_{\alpha}\]

\(x+1\) 的映射可以有许多不同的情况。

在最简单的情况下，

\[x+1\ \ \mapsto(x_{0}+1,x_{1},...,x_{\alpha},x_{\alpha+1})\]

那么我们有

$$\begin{aligned}
f_{B}(x + 1) &= x_0 + 1 + x_1 \cdot N_0 d_0 + x_2 \cdot N_1 d_1 + \ldots + x_{\alpha} \cdot N_{\alpha - 1} d_{\alpha - 1} + x_{\alpha + 1} \cdot N_{\alpha} d_{\alpha} \\
&= f_{B}(x) + 1 \\
&> f_{B}(x)
\end{aligned}$$

在一个更复杂的情况下，其中 \(x_{0}=d_{0}-1\) 且 \(x_{1}<\frac{d_{1}}{N_{0}d_{0}}-1\)，我们有

\[x+1\ \ \mapsto(0,x_{1}+1,...,x_{\alpha},x_{\alpha+1})\]

那么我们有

$$\begin{aligned}
f_{B}(x + 1) &= 0 + (x_1 + 1) \cdot N_0 d_0 + x_2 \cdot N_1 d_1 + \ldots + x_{\alpha} \cdot N_{\alpha - 1} d_{\alpha - 1} + x_{\alpha + 1} \cdot N_{\alpha} d_{\alpha} \\
&= f_{B}(x) - x_0 + N_0 d_0 \\
&= f_{B}(x) - (d_0 - 1) + N_0 d_0 \\
&= f_{B}(x) + 1 + (N_0 - 1) d_0 \\
&> f_{B}(x)
\end{aligned}$$

因为 \(N_{0}\geq 1\)，我们有 \((N_{0}-1)d_{0}\geq 0\)，所以

\[f_{B}(x+1)\ \ >f_{B}(x)\]

一般情况下，当 \(x_{0}\,=\,d_{0}-1\)，对于某个 \(k\,\in\,[1,\alpha-1]\)，每个 \(i\,\in\,[1,k]\) 有 \(x_{i}\,=\,\frac{d_{i}}{N_{i-1}d_{i-1}}-1\)，\(x_{k+1}<\frac{d_{k+1}}{N_{k}d_{k}}-1\)，我们有

\[x+1\ \ \mapsto(0,0,...,0,x_{k+1}+1,...,x_{\alpha},x_{\alpha+1})\]

那么我们有

$$\begin{aligned}
f_{B}(x + 1) &= 0 + 0 \cdot N_0 d_0 + \ldots + 0 \cdot N_{k - 1} d_{k - 1} + (x_{k + 1} + 1) \cdot N_k d_k + \ldots + x_{\alpha} \cdot N_{\alpha - 1} d_{\alpha - 1} + x_{\alpha + 1} \cdot N_{\alpha} d_{\alpha} \\
&= f_{B}(x) - x_0 - \left(\sum_{i = 1}^{k} x_i \cdot N_{i - 1} d_{i - 1}\right) + N_k d_k \\
&= f_{B}(x) - (d_0 - 1) - \left(\sum_{i = 1}^{k} \left(\frac{d_i}{N_{i - 1} d_{i - 1}} - 1\right) \cdot N_{i - 1} d_{i - 1}\right) + N_k d_k \\
&= f_{B}(x) - (d_0 - 1) - \left(\sum_{i = 1}^{k} \left(d_i - N_{i - 1} d_{i - 1}\right)\right) + N_k d_k \\
&= f_{B}(x) - (d_0 - 1) + \sum_{i = 1}^{k} N_{i - 1} d_{i - 1} - \sum_{i = 1}^{k} d_i + N_k d_k \\
&=  f_{B}(x) + \sum_{i = 0}^{k} \left(N_{i} - 1\right) d_{i} + 1 \\
\end{aligned}$$

因为对于每个 \(i\)，\(N_{i}\geq 1\)，所以对于每个 \(i\)，\((N_{i}-1)\,d_{i}\geq 0\)，因此

\[f_{B}(x+1) >f_{B}(x)\]

证明完毕。

类似地，我们也可以证明 \(\{A,M\}\) 的补集的扩展是严格递增的。

**命题 2.7**：令 \(\{A=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha}),M\}\) 对于补集可接纳，且 \(B=\text{complement}(A,M)\)。令 \(C=(A,\bar{B})\) 为连接的布局。那么 \(C\) 的大小为 \(M\)，且 \(f_{C}:[0,M)\to\mathbb{N}\) 限制为双射 \([0,M)\cong[0,M)\)。

证明

因为 \(\text{size}(A)=\prod\limits_{i=0}^{\alpha}N_{i}\) 且 \(\text{size}(B)=\frac{M}{\underset{i=0}{\overset{\alpha}{\bot}}}\,,\) 我们有 \(\text{size}(C)=\text{size}(A)\cdot\text{size}(B)=M.\) 因此 \(f_{C}\) 的定义域是 \([0,M)\)。

注意 \(f_{C}\) 的像与 \(f_{C^{\prime}}\) 的像相同，其中 \(C^{\prime}\) 是 \(C\) 的任何排列。

要看到这一点，假设我们有如下布局 \(C\) 及其排列 \(C^{\prime}\)，其中只有一对模式被交换。

\[\begin{array}{lll}C&=(N_{0},N_{1},...,N_{i},...,N_{j},...,N_{\alpha}):(d_{0 },d_{1},...,d_{i},...,d_{j},...,d_{\alpha})\\ C^{\prime}&=(N_{0},N_{1},...,N_{j},...,N_{i},...,N_{\alpha}):(d_{0},d_{1}, ...,d_{j},...,d_{i},...,d_{\alpha})\end{array}\]

\(f_{C}\) 和 \(f_{C}^{\prime}\) 的定义域都是 \([0,M).\) 对于任何 \(x_{C}\in[0,M),\) 我们有

\[\begin{array}{lll}x_{C}&\mapsto(x_{0},x_{1},...,x_{i},...,x_{j},...,x_{\alpha })\\ x_{C^{\prime}}&\mapsto(x_{0},x_{1},...,x_{j},...,x_{i},...,x_{\alpha})\end{array}\]

且 \(x_{C}\) 和 \(x_{C^{\prime}}\) 是双射的。

因为根据定义，\(f_{C}(x_{C})=f_{C^{\prime}}(x_{C^{\prime}})\)，所以 \(f_{C}\) 的像与 \(f_{C^{\prime}}\) 的像相同。

对于 \(C\) 的任何排列 \(C^{\prime}\)，它可以通过一次交换 \(C\) 的一对模式而得到，并且每次 \(f_{C}\) 的像与 \(f_{C^{\prime}}\) 的像相同。因此，对于 \(C\) 的任何排列 \(C^{\prime}\)，\(f_{C}\) 的像与 \(f_{C^{\prime}}\) 的像相同。

计算 \(f_{C}\) 的像时，我们可以对 \(C\) 进行排序。不失一般性，假设 \(A=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\) 已经排序。对 \(C\) 排序后，排序后的 \(C^{\prime}\) 只能如下：

\[C^{\prime} = \left(d_{0},N_{0},\frac{d_{1}}{N_{0}d_{0}},N_{1},\frac{d_{2}}{N_{1 }d_{1}},N_{2},...,\frac{d_{\alpha}}{N_{\alpha-1}d_{\alpha-1}},N_{\alpha},\frac{ M}{N_{\alpha}d_{\alpha}}\right):(1,d_{0},N_{0}d_{0},d_{1},N_{1}d_{1},d_{2},...\, ,N_{\alpha-1}d_{\alpha-1},d_{\alpha},N_{\alpha})\]

因为对于每个 \(i\)，有 \(d_{i}\leq N_{i}d_{i}\) 且 \(N_{i}d_{i}\leq d_{i+1}\)，当 \(N_{i}=1\) 时，\(N_{i}\leq\frac{d_{i+1}}{N_{i}d_{i}}\)，当 \(N_{i}d_{i}=d_{i+1}\) 时，\(\frac{d_{i+1}}{N_{i}d_{i}}\leq N_{i+1}\)，因此 \(C^{\prime}\) 是排序的，并且 \(C^{\prime}\) 的任何排列都会使其不再排序。

然后我们可以重写

\[C^{\prime} = (r_{0},r_{1},r_{2},...,r_{\beta}):(1,r_{0},r_{0}r_{1},...,r_{0}r_{ 1}...r_{\beta-1})\]

其中 \(\beta=2\alpha+1\)，且 \(f_{C^{\prime}}\) 达到的最大值计算如下：

$$\begin{aligned}
f_{C^{\prime}}(M - 1) &= f_{C^{\prime}}(r_0 - 1, r_1 - 1, r_2 - 1, \ldots, r_{\beta - 1} - 1, r_{\beta} - 1) \\
&= (r_0 - 1) + (r_1 - 1) \cdot r_0 + (r_2 - 1) \cdot r_0 r_1 + \ldots + (r_{\beta - 1} - 1) \cdot r_0 r_1 \ldots r_{\beta - 2} + (r_{\beta} - 1) \cdot r_0 r_1 \ldots r_{\beta - 1} \\
&= r_0 - 1 + r_0 r_1  - r_0 + r_0 r_1 r_2 - r_0 r_1 + \ldots + r_0 r_1 \ldots r_{\beta - 1} - r_0 r_1 \ldots r_{\beta - 2} + r_0 r_1 \ldots r_{\beta} - r_0 r_1 \ldots r_{\beta - 1} \\
&= r_0 r_1 \ldots r_{\beta} - 1 \\
&= M - 1
\end{aligned}$$

那么在这种情况下，要建立双射性断言，只需证明 \(f_{C^{\prime}}(x)\) 是单射的，即对于任何 \(x,y\in[0,M)\)，如果 \(f_{C^{\prime}}(x)=f_{C^{\prime}}(y)\)，则 \(x=y\)。

假设 \(x\) 和 \(y\) 的同构映射如下：

\[\begin{array}{lll}x&\mapsto(x_{0},x_{1},...,x_{\beta})\\ y&\mapsto(y_{0},y_{1},...,y_{\beta})\end{array}\]

因为 \(f_{C^{\prime}}(x)=f_{C^{\prime}}(y)\)，我们有

\[x_{0}+x_{1}\cdot r_{0}+x_{2}\cdot r_{0}r_{1}+...+x_{\beta}\cdot r_{0}r_{1}.. .r_{\beta-1}=y_{0}+y_{1}\cdot r_{0}+y_{2}\cdot r_{0}r_{1}+...+y_{\beta}\cdot r _{0}r_{1}...r_{\beta-1}\]

我们将使用强归纳法证明对于每个 \(i\in[0,\beta]\)，有 \(x_{i}=y_{i}\)。

因为 \(f_{C^{\prime}}(x) mod~{}r_{0}=f_{C^{\prime}}(y) mod~{}r_{0}\)，我们有 \(x_{0}=y_{0}\)。

现在假设通过强归纳法，给定 \(i\in(0,\beta]\)，对于所有 \(j<i\)，我们有 \(x_{j}=y_{j}\)。我们有

\[x_{i}\cdot r_{0}r_{1}...r_{i-1}+x_{i+1}\cdot r_{0}r_{1}...r_{i}+...+x_{\beta} \cdot r_{0}r_{1}...r_{\beta-1}=y_{i}\cdot r_{0}r_{1}...r_{i-1}+y_{i+1}\cdot r_{0 }r_{1}...r_{i}+...+y_{\beta}\cdot r_{0}r_{1}...r_{\beta-1}\]

因为 \(x_{i}\in[0,r_{i})\) 且 \(y_{i}\in[0,r_{i})\)，将此等式对 \(r_{0}r_{1}...r_{i}\) 取模并除以 \(r_{0}r_{1}...r_{i-1}\)，我们得到 \(x_{i}=y_{i}\)。

因为 \((x_{0},x_{1},...,x_{\beta})=(y_{0},y_{1},...,y_{\beta})\)，且同构映射是双射的，我们有 \(x=y\)。

因此 \(f_{C^{\prime}}:[0,M)\to\mathbb{N}\) 限制为双射 \([0,M)\cong[0,M)\)。\(f_{C}\) 也是如此。

证明完毕。

**推论 2.8** **补集不相交性**

推论 2.8 解释了取布局的补集意味着什么。

在命题 2.7 的设定中，令 \(I=[0,\text{size}(A))=[0,N_{0}N_{1}...N_{\alpha})\) 为 \(f_{A}\) 的定义域。那么

\[f_{A}(I)\cap\widehat{f}_{B}(I)=\{0\}\]

换句话说，当限制在 \(f_{A}\) 的定义域上时，\(\widehat{f}_{A}\) 和 \(\widehat{f}_{B}\) 的像除了 \(0\) 以外是不相交的。

注意在推论中，\(f_{A}\) 和 \(\widehat{f}_{A}\) 实际上是可以互换的，因为函数定义域被限制在 \(f_{A}\) 的定义域上。

证明

令 \(J=[0,\text{size}(B))=[0,\frac{M}{N_{0}N_{1}...N_{\alpha}})\) 为 \(f_{B}\) 的定义域。那么根据命题 2.7，我们有

\[f_{A}(I)\cap f_{B}(J)=\{0\}\]

为了理解这一点，对于任何 \(x_{A}\in I\) 和任何 \(x_{B}\in J\)，由于同构，我们有

\[\begin{array}{ll}x_{A}&\mapsto(x_{A,0},x_{A,1},...,x_{A,\alpha})\\ x_{B}&\mapsto(x_{B,0},x_{B,1},...,x_{B,\alpha},x_{B,\alpha+1})\end{array}\]

那么我们有

\[\begin{array}{ll}f_{A}(x_{A})&=x_{A,0}+x_{A,1}\cdot N_{0}+x_{A,2}\cdot N_{0}N_ {1}+...+x_{A,\alpha}\cdot N_{0}N_{1}...N_{\alpha-1}\\ f_{B}(x_{B})&=x_{B,0}+x_{B,1}\cdot N_{0}d_{0}+x_{B,2}\cdot N_{1}d_{1}+...+x_{B, \alpha}\cdot N_{\alpha-1}d_{\alpha-1}+x_{B,\alpha+1}\cdot N_{\alpha}d_{\alpha }\end{array}\]

我们为布局 \(C\) 编排新坐标如下：

\[\begin{array}{ll}x_{A}^{\prime}&\mapsto\left(0,x_{A,0},0,x_{A,1},0,x_{A,2},.. .,0,x_{A,\alpha},0\right)\\ x_{B}^{\prime}&\mapsto\left(x_{B,0},0,x_{B,1},0,x_{B,2},...,x_{B,\alpha},0,x _{B,\alpha+1}\right)\end{array}\]

那么我们有

\[\begin{array}{ll}f_{C}(x_{A}^{\prime})&=x_{A,0}+x_{A,1}\cdot N_{0}+x_{A,2} \cdot N_{0}N_{1}+...+x_{A,\alpha}\cdot N_{0}N_{1}...N_{\alpha-1}\\ &=f_{A}(x_{A})\\ f_{C}(x_{B}^{\prime})&=x_{B,0}+x_{B,1}\cdot N_{0}d_{0}+x_{B,2}\cdot N_{1}d_{1}+ ...+x_{B,\alpha}\cdot N_{\alpha-1}d_{\alpha-1}+x_{B,\alpha+1}\cdot N_{ \alpha}d_{\alpha}\\ &=f_{B}(x_{B})\end{array}\]

根据命题 2.7，我们有 \(f_{C}:[0,M)\to\mathbb{N}\) 限制为双射 \([0,M)\cong[0,M)\)。如果 \(x_{A}^{\prime}\neq x_{B}^{\prime}\)，则 \(f_{C}(x_{A}^{\prime})\neq f_{C}(x_{B}^{\prime})\)，且 \(f_{A}(x_{A})\neq f_{B}(x_{B})\)。

显然，除了 \((0, 0, … , 0)\)，对于 $x_{A,0},x_{A,1},\ldots,x_{A,\alpha}$ 和 $x_{B,0},x_{B,1},\ldots,x_{B,\alpha},x_{B,\alpha + 1}$, $\left( 0,x_{A,0},0,x_{A,1},0,x_{A,2},\ldots,0,x_{A,\alpha},0 \right) \neq \left( x_{B,0},0,x_{B,1},0,x_{B,2},\ldots,x_{B,\alpha},0,x_{B,\alpha + 1} \right)$, $x_{A}^{\prime} \neq x_{B}^{\prime}$, $f_{C}(x_{A}^{\prime}) \neq f_{C}(x_{B}^{\prime})$,，且 \(𝑓𝐴(𝑥𝐴) ≠𝑓𝐵(𝑥𝐵)\)。

这意味着，对于任何 \(𝑥∈𝐼\) 且 \(𝑥≠0\)，不存在 𝑦∈𝐽 使得 \(𝑓𝐴(𝑥) = 𝑓𝐵(𝑦)\)。
当 \(𝑥= 0\) 时，我们有 \(𝑓𝐴(𝑥) = 𝑓𝐵(𝑥) = 0\)。因此我们可以断言

$$\begin{array}{r}
{f_{A}(I) \cap f_{B}(J) = \{ 0\}}
\end{array}$$

在定义 2.6: 补集中，我们已经证明了 \(\{𝐴, 𝑀\}\) 的补集 \(𝑓𝐵\) 及其扩展𝑓𝐵 是严格递增的。
此外，根据同构的扩展，我们有

$$\begin{array}{r}
{\text{size}(B)\mapsto\left( 0,0,\ldots,0,\frac{M}{N_{\alpha}d_{\alpha}} \right)}
\end{array}$$

那么我们有

$$\begin{matrix}
{{\hat{f}}_{B}(\text{size}(B))} & {= 0 + 0 \cdot 1 + 0 \cdot N_{0}d_{0} + \ldots + 0 \cdot N_{\alpha - 1}d_{\alpha - 1} + \frac{M}{N_{\alpha}d_{\alpha}} \cdot N_{\alpha}d_{\alpha}} \\
 & {= M}
\end{matrix}$$

达到的最大值在 \(𝑁0𝑁1 … 𝑁𝛼−1\) 处，且 \(𝑓𝐴(𝑁0𝑁1 … 𝑁𝛼−1) = (𝑁0 −1)𝑑0 +
(𝑁1 −1)𝑑1 + … + (𝑁𝛼−1)𝑑𝛼\)。
因为对于每个 \(𝑖∈[0, 𝛼−1]\)，有 \((𝑁0 −1)𝑑0 < 𝑁0𝑑0\) 且 \(𝑁𝑖𝑑𝑖≤𝑑𝑖+1\)，以及 $N_{\alpha}d_{\alpha} \leq M$，我们有

$$\begin{aligned}
{f_{A}(N_{0}N_{1}\ldots N_{\alpha} - 1)} & {= (N_{0} - 1)d_{0} + (N_{1} - 1)d_{1} + \ldots + (N_{\alpha} - 1)d_{\alpha}} \\
 & {< N_{0}d_{0} + N_{1}d_{1} - d_{1} + N_{2}d_{2} - d_{2} + \ldots + N_{\alpha}d_{\alpha} - d_{\alpha}} \\
 & {\leq d_{1} + N_{1}d_{1} - d_{1} + N_{2}d_{2} - d_{2} + \ldots + N_{\alpha}d_{\alpha} - d_{\alpha}} \\
 & {= N_{1}d_{1} + N_{2}d_{2} - d_{2} + \ldots + N_{\alpha}d_{\alpha} - d_{\alpha}} \\
 & {\leq d_{2} + N_{2}d_{2} - d_{2} + \ldots + N_{\alpha}d_{\alpha} - d_{\alpha}} \\
 & {\vdots} \\
 & {\leq d_{\alpha} + N_{\alpha}d_{\alpha} - d_{\alpha}} \\
 & {= N_{\alpha}d_{\alpha}} \\
 & {\leq M}
\end{aligned}$$

因此 $f_{A}(N_{0}N_{1}\ldots N_{\alpha} - 1) < {\hat{f}}_{B}(\text{size}(B))$。
在 $I \cap J = I$ 的情况下，即 $\text{size}(A) \leq \text{size}(B)$。那么我们有

$$\begin{array}{r}
{f_{A}(I) \cap f_{B}(I) = \{ 0\}}
\end{array}$$

因为在这种情况下， $f_{B}(I) = {\hat{f}}_{B}(I)$ ，我们有

$$\begin{array}{r}
{f_{A}(I) \cap {\hat{f}}_{B}(I) = \{ 0\}}
\end{array}$$

在另一种情况 $I \cap J = J$ 下，即  $\text{size}(A) \geq \text{size}(B)$。因为  $f_{A}$ 达到的最大值是 $f_{A}(N_{0}N_{1}\ldots N_{\alpha} - 1)$，且  $f_{A}(N_{0}N_{1}\ldots N_{\alpha} - 1) < {\hat{f}}_{B}(\text{size}(B))$，对于任何 $x \in I/J$，我们有
$f_{A}(x) < {\hat{f}}_{B}(\text{size}(B))$。

因此，

\[f_{A}(I)\cap\hat{f}_{B}(I/J)=\emptyset\]

所以，

\[f_{A}(I)\cap\hat{f}_{B}(I) =f_{A}(I)\cap\left(\hat{f}_{B}(I)\cup\hat{f}_{B}(I/J)\right)\] \[=f_{A}(I)\cap\left(f_{B}(I)\cup\hat{f}_{B}(I/J)\right)\] \[=\left(f_{A}(I)\cap f_{B}(I)\right)\cup\left(f_{A}(I)\cap\hat{f}_{ B}(I/J)\right)\] \[=\left\{0\right\}\cup\emptyset\] \[=\left\{0\right\}\]

综上所述，我们有

\[f_{A}(I)\cap\hat{f}_{B}(I)=\left\{0\right\}\]

证明完毕。

关于原论文中推论 2.8 证明的一个简短说明是，Jay Shah 声称 \(f_{A}(I\cap J)\cap f_{B}(I\cap J)=\left\{0\right\}\)，这不足以完成证明。充分的陈述应该是 \(f_{A}(\hat{I})\cap f_{B}(J)=\left\{0\right\}\)。

**备注 2.9** **补集不相交性、排序性和有界性**

布局 \(A\) 关于整数 \(M\) 的补集 \(B\) 应满足三个性质：

1.  \(A\) 和 \(B\) 在以下意义下不相交：对于所有 \(x\neq 0\) 和 \(\hat{f}_{A}\) 定义域中的 \(y\)，有 \(f_{A}(x)\neq\hat{f}_{B}(y)\)。
2.  \(\hat{B}\) 是有序的，即 \(f_{B}(x)\) 是一个严格递增函数。
3.  \(B\) 以 \(M\) 为界，即 \(\operatorname{size}(B)\geq\frac{M}{\operatorname{size}(A)}\) 且 \(\operatorname{cosize}(B)\leq\left|\frac{M}{\operatorname{cosize}(A)}\right| \cdot\operatorname{cosize}(A)\)。这里布局 \(L\) 的 \(\operatorname{cosize}\) 定义为 \(\operatorname{cosize}(L)=f_{L}(\operatorname{size}(L)-1)+1\)。

性质 1 和 2 已经在推论 2.8 和定义 2.6 中得到证明。我们将展示性质 3 的证明。

证明

根据定义 2.6，我们有 \(\operatorname{size}(B)=\frac{M}{\operatorname{size}(A)}\)。

因为 \(\operatorname{cosize}\) 对布局的顺序不敏感，不失一般性，我们对 \(A\) 排序，使得 \(A=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\)。

根据 \(\operatorname{cosize}\) 的定义，我们有

$$\begin{aligned}
{\text{cosize}(B)} & {= f_{B}(\text{size}(B) - 1) + 1} \\
 & {= f_{B}\left( d_{0} - 1,\frac{d_{1}}{N_{0}d_{0}} - 1,\ldots,\frac{d_{\alpha}}{N_{\alpha - 1}d_{\alpha - 1}} - 1,\frac{M}{N_{\alpha}d_{\alpha}} - 1 \right) + 1} \\
 & {= (d_{0} - 1) + \left( \frac{d_{1}}{N_{0}d_{0}} - 1 \right) \cdot N_{0}d_{0} + \ldots + \left( \frac{d_{\alpha}}{N_{\alpha - 1}d_{\alpha - 1}} - 1 \right) \cdot N_{\alpha - 1}d_{\alpha - 1} + \left( \frac{M}{N_{\alpha}d_{\alpha}} - 1 \right) \cdot N_{\alpha}d_{\alpha} + 1} \\
 & {= d_{0} + d_{1} + \ldots + d_{\alpha} - N_{0}d_{0} - N_{1}d_{1} - \ldots - N_{\alpha}d_{\alpha} + M} \\
 & {= M - \left( \left( N_{0} - 1 \right)d_{0} + \left( N_{1} - 1 \right)d_{1} + \ldots + \left( N_{\alpha} - 1 \right)d_{\alpha} \right)} \\
 & {= M - f_{A}(\text{size}(A) - 1)} \\
 & {= M - \left( \text{cosize}(A) - 1 \right)}
\end{aligned}$$

为了得到不等式 $\text{cosize}(B) \leq \left\lfloor \frac{M}{\text{cosize}(A)} \right\rfloor \cdot \text{cosize}(A)$，我们将上述等式除以 $\text{cosize}(A)$。

$$\begin{aligned}
\frac{\text{cosize}(B)}{\text{cosize}(A)} & {= \frac{M - \left( \text{cosize}(A) - 1 \right)}{\text{cosize}(A)}} \\
 & {= \frac{M}{\text{cosize}(A)} - 1 + \frac{1}{\text{cosize}(A)}}
\end{aligned}$$

我们需要证明

\[\frac{M}{\text{cosize}(A)}-1+\frac{1}{\text{cosize}(A)}\leq\left\lfloor\frac{M}{ \text{cosize}(A)}\right\rfloor\]

事实上，对于任何 \(a,b\in\mathbb{N}\) 且 \(a\geq 1\)，我们有

\[\frac{b}{a}-1+\frac{1}{a}\leq\left\lfloor\frac{b}{a}\right\rfloor\]

要看到这一点，假设 \(\frac{b}{a}=\left\lfloor\frac{b}{a}\right\rfloor+c\)，其中 \(c=\frac{k}{a}\)，\(k\) 是一个整数，满足 \(0\leq k<a\)。那么我们有
\(\frac{1}{a}\leq c<1\) 且 \(1\leq ac<a\)。我们想要证明

\[\frac{b}{a}-1+\frac{1}{a}\leq\frac{b}{a}-c\]

\[-a+1\leq-ac\]

\[a-ac\geq 1\]

\[a-k\geq 1\]

因为 \(a\) 和 \(k\) 都是整数，且 \(0\leq k<a\)，我们有 \(a-k\geq 1\)。因此不等式成立。证明完毕。

### 非整数布局补集

上述所有补集的性质和证明都假设被补的布局是一个模式为整数的布局，即 \(A=(N_{0},N_{1},...,N_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\)。在布局非整数的情况下，即某些模式不是整数，应在应用补集之前将布局合并为整数布局。这是有效的，因为合并不改变布局函数。

### 补集的含义

布局的补集找到了一个具有正整数的补集布局，使得当两个布局连接时，例如 \((B,\text{complement}(B,M))\)，新布局是一个双射 \([0,M)\ \cong\ [0,M)\)。这也就是说，如果使用补集布局重复原始布局，新布局仍然是双射。

## 复合

**定义 2.11 左可除性**

令 \(M,d>0\) 为正整数，并令 \(M=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha}\) 为由整数 \(M_{k}>1\) 给出的 \(M\) 的因式分解，\(k\in[0,\alpha]\)。将 \(M_{\alpha}\) 替换为 \(\infty\)，令

\[\hat{M}=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\cdot\infty\]

并考虑 \(\infty\) 可被每个正整数整除。如果存在 \(0\leq i\leq\alpha\) 使得以下条件成立，我们说 \(M\) 被 \(d\) 左可除（隐式地，关于给定的因式分解）：

1.  \(M_{0}\cdot M_{1}\cdot...\cdot M_{i-1}\) 整除 \(d\)。
2.  假设第一个条件满足。令 \(c=\frac{d}{M_{0}\cdot M_{1}\cdot...\cdot M_{i-1}}\)。那么如果 \(i<\alpha\)，我们额外要求 \(1\leq c<M_{i}\)。
3.  对于 \(i<\alpha\) 的情况下的第二个条件，我们额外要求 \(c\) 也整除 \(M_{i}\)。

这里 \(i\) 如果存在则必然是唯一的。我们可以通过反证法证明这一点。

```
注释：
从d找到其位于M的第i个维度 
```

证明

假设存在两个不同的 \(i\) 和 \(j\) 满足这三个条件。不失一般性，假设 \(i<j\)。

有两种情况需要考虑。

在 \(j<\alpha\) 的情况下，我们也会有 \(i<\alpha\)。那么我们有

\[d =c\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{i-1}\]

其中 \(c\) 是某个正整数，满足 \(1\leq c<M_{i}\)。

类似地，

\[d =c^{\prime}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{j-1}\]

其中 \(c^{\prime}\) 是某个正整数，满足 \(1\leq c^{\prime}<M_{j}\)。

因此，

\[c\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{i-1} =c^{\prime}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{j-1}\]

\[c =c^{\prime}\cdot M_{i}\cdot M_{i+1}\cdot...\cdot M_{j-1}\]

为了使上述等式成立，我们必须证明

\[c^{\prime}\cdot\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{j-1}=1\]

然而，因为对于 \(k\in[0,\alpha]\)，\(M_{k}>1\)，\(\frac{M_{i}}{c}>1\)，且 \(c^{\prime}\geq 1\)，上述等式不可能成立。这产生了矛盾。因此，\(i\) 是唯一的。

在 \(j=\alpha\) 的情况下，我们也会有 \(i<\alpha\)。那么我们有

\[d =c\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{i-1}\]

其中 \(c\) 是某个正整数，满足 \(1\leq c<M_{i}\)。

类似地，

\[d =c^{\prime}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\]

其中 \(c^{\prime}\) 是某个正整数。

因此，

\[c\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{i-1} =c^{\prime}\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\]

\[c =c^{\prime}\cdot M_{i}\cdot M_{i+1}\cdot...\cdot M_{\alpha-1}\]

为了使上述等式成立，我们必须证明

\[c^{\prime}\cdot\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{\alpha-1}=1\]

然而，因为对于 \(k\in[0,\alpha]\)，\(M_{k}>1\)，\(\frac{M_{i}}{c}>1\)，且 \(c^{\prime}\geq 1\)，上述等式不可能成立。这产生了矛盾。因此，\(i\) 是唯一的。

综上所述，如果 \(i\) 存在，则它是唯一的。

证明完毕。

如果 \(i\) 存在，我们将 \(i\) 称为除法索引，并写作 \(\hat{M}=d\cdot\hat{M}^{\prime}\)，其中 \(\hat{M}^{\prime}\) 被赋予以下诱导因式分解：

1.  如果 \(0\leq i<\alpha\)，那么 \(\hat{M}^{\prime}=\hat{M}_{0}^{\prime}\cdot\hat{M}_{1}^{\prime}\cdot...\cdot \hat{M}_{\alpha-i-1}^{\prime}\cdot\infty\)，其中 \(\hat{M}_{0}^{\prime}=\frac{M_{i}}{c}>1\) 且对于 \(0<j<\alpha-i\)，\(\hat{M}_{j}^{\prime}=M_{i+j}\)。
2.  如果 \(i=\alpha\)，那么 \(\hat{M}=d\cdot\infty\)，我们将令 \(\hat{M}^{\prime}=\infty\)。

要看到这一点，在 \(0\leq i<\alpha\) 的情况下，我们有

$$\begin{aligned}
\widehat{M} &= M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1} \cdot \infty \\
&= M_{0} \cdot M_{1} \cdot \ldots \cdot M_{i - 1} \cdot M_{i} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 1} \cdot \infty \\
&= \frac{d}{c} \cdot M_{i} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 1} \cdot \infty \\
&= d \cdot \frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 1} \cdot \infty \\
&= d \cdot M_{0}^{\prime} \cdot M_{1}^{\prime} \cdot \ldots \cdot M_{\alpha - i - 1}^{\prime} \cdot \infty \\
&= d \cdot \widehat{M}^{\prime}
\end{aligned}$$

其中 \(\hat{M}^{\prime}=M_{0}^{\prime}\cdot M_{1}^{\prime}\cdot...\cdot M_{\alpha-i-1 }^{\prime}\cdot\infty\)，且 \(M_{0}^{\prime}=\frac{M_{i}}{c}>1\)，对于 \(0<j<\alpha-i\)，\(M_{j}^{\prime}=M_{i+j}\)。

在 \(i=\alpha\) 的情况下，我们有

$$\begin{aligned}
\widehat{M} &= M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1} \cdot \infty \\
&= \frac{d}{c} \cdot \infty \\
&= d \cdot \infty \\
&= d \cdot \widehat{M}^{\prime}
\end{aligned}$$

其中 \(\hat{M}^{\prime}=\infty\)。

此外，如果存在 \(0\leq i\leq\alpha\) 使得左可除性的条件 1 和 2 被满足，但不一定满足条件 3，我们说 \(M\) 被 \(d\) 弱左可除。

注意在除法索引 \(i\) 的唯一性证明中，我们从未使用条件 3。因此，对于弱左可除性，我们仍然可以有唯一必要的除法索引 \(i\)，但我们不再有 \(\hat{M}\) 的因式分解，因为因式分解假设了左可除性的条件 3。

还要注意，具有其诱导因式分解的 \(\hat{M}^{\prime}\) 本身可以考虑用于左可除性或弱左可除性（现在将最后一个因子替换为 \(\infty\) 的步骤是多余的）。更具体地说，因为 \(\hat{M}^{\prime}>0\)，对于 \(j\in[0,\alpha-i-1]\)，\(\hat{M}_{j}^{\prime}>1\)，且 \(\hat{M}^{\prime}=\hat{M}_{0}^{\prime}\cdot\hat{M}_{1}^{\prime}\cdot...\cdot\hat{ M}_{\alpha-i-1}^{\prime}\cdot\infty\)，给定另一个正整数 \(d^{\prime}>0\)，我们可以完全测试左可除性或弱左可除性的性质是否对关于 \(d^{\prime}\) 的 \(\hat{M}^{\prime}\) 成立。将最后一个因子替换为 \(\infty\) 是不必要的，因为它已经是 \(\infty\)。

**定义 2.12** **复合的可接纳性 - 受限情况**

我们首先考虑第二种布局长度为 1 的受限情况下的复合。

令 \({\bf S}=(M_{0},M_{1},...,M_{\alpha})\) 为形状元组，令 \(M=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha}\)，令 \(B=(N):(r)\) 为长度为 1 的布局。那么如果满足以下条件，我们说对 \(\{{\bf S},B\}\) 对于复合是可接纳的（或简称为可接纳）：

1.  \(M\) 被 \(r\) 左可除。写作 \(\hat{M}=r\cdot\hat{M}^{\prime}\)。
2.  关于其诱导因式分解，\(\hat{M}^{\prime}\) 被 \(N\) 弱左可除。

**定义 2.13** **复合 - 受限情况**

可接纳性的思想是布局 \(A\circ B\) 的复合将涉及“沿着 \(A\) 的模式划分 \(B\)”。更精确地说，我们有如下内容：

假设 \({\bf S}=(M_{0},M_{1},...,M_{\alpha})\) 是一个形状元组，且 \(B=(N):(r)\) 是一个长度为 1 的布局，使得 \(\{{\bf S},B\}\) 对于复合是可接纳的。令 \({\bf D}=(d_{0},d_{1},...,d_{\alpha})\) 为任何步长元组，并令 \(A=({\bf S}:{\bf D})\) 为一个**合并的**布局。

注意在 Jay Shah 的原论文中，布局 \(A\) 没有被指定为合并的。这将导致一些复合无效。

如定义 2.11 中，令 \(M=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha}\) 且 \(\hat{M}=r\cdot\hat{M}^{\prime}\)，除法索引为 \(0\leq i\leq\alpha\)。我们将 \(A\circ B\) 的定义分为两种情况。

首先假设 \(0\leq i<\alpha\)，那么

\[\begin{array}{lll}r&=M_{0}\cdot M_{1}\cdot...\cdot M_{i-1}\cdot c\\ \hat{M}^{\prime}&=\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{\alpha-1}\cdot \infty\end{array}\]

那么如果 \(N\leq\frac{M_{i}}{c}\)，我们令 \(A\circ B=(N):(cd_{i})\)。

否则，存在一个 \(j\in[i+1,\alpha]\) 使得 \(N=\frac{M_{i}}{c}\cdot...\cdot M_{j-1}\cdot c^{\prime}\)，其中如果 \(j\neq\alpha\)，则 \(1\leq c^{\prime}<M_{j}\)（当 \(j=i+1\) 时，\(N=\frac{M_{i}}{c}\cdot c^{\prime}\)）。

注意这里有一个重要事实，即 \(c^{\prime}\) 必须是整数，因为复合可接纳性的第二个条件，即 \(\hat{M}^{\prime}\) 被 \(N\) 弱左可除。我们必须有 \(\frac{M_{i}}{c}\cdot...\cdot M_{j-1}\) 整除 \(N\)，导致 \(c^{\prime}\) 是整数。

我们令

$$\begin{array}{r}
{A \circ B = \left\{ \begin{matrix}
{\left( \frac{M_{i}}{c},M_{i + 1},\ldots,M_{j - 1},c^{\prime} \right):\left( cd_{i},d_{i + 1},\ldots,d_{j - 1},d_{j} \right)} & {\text{if~}c^{\prime} > 1} \\
{\left( \frac{M_{i}}{c},M_{i + 1},\ldots,M_{j - 1} \right):\left( cd_{i},d_{i + 1},\ldots,d_{j - 1} \right)} & {\text{if~}c^{\prime} = 1}
\end{matrix} \right.}
\end{array}$$

如果 \(i=\alpha\)，那么我们有 \(r=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\cdot c\) 如前，但 \(\widehat{M}^{\prime}=\infty\)，我们令 \(A\circ B=(N):(cd_{\alpha})\)。

让我们更仔细地看这个定义。

本质上，我们正在沿着布局 \(A\) 取一维坐标 \(k\cdot r\)，其中 \(k\in[0,N-1]\)。因为我们有 \(r=M_{0}\cdot M_{1}\cdot...\cdot M_{i-1}\cdot c\)，且 \(c\) 整除 \(M_{i}\)。

首先考虑 \(0\leq i<\alpha\) 的情况。

如果 \(N\leq\frac{M_{i}}{c}\)，那么布局 \(A\) 中的第一个模式足以划分 \(B\)。因此，复合布局 \(A\circ B=(N):(cd_{i})\)。

否则，如果 \(N>\frac{M_{i}}{c}\)，布局 \(A\) 中更多的模式将参与划分 \(B\)，因此复合布局

$$\begin{array}{r}
{A \circ B = \left\{ \begin{matrix}
{\left( \frac{M_{i}}{c},M_{i + 1},\ldots,M_{j - 1},c^{\prime} \right):\left( cd_{i},d_{i + 1},\ldots,d_{j - 1},d_{j} \right)} & {\text{if~}c^{\prime} > 1} \\
{\left( \frac{M_{i}}{c},M_{i + 1},\ldots,M_{j - 1} \right):\left( cd_{i},d_{i + 1},\ldots,d_{j - 1} \right)} & {\text{if~}c^{\prime} = 1}
\end{matrix} \right.}
\end{array}$$

然后考虑 \(i=\alpha\) 的情况。我们有 \(r=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\cdot c\) 且 \(\widehat{M}^{\prime}=\infty\)。这里 \(c\) 是一个可以无限大的正整数。所以只有布局 \(A\) 中的最后一个模式参与划分 \(B\)，因此复合布局 \(A\circ B=(N):(cd_{\alpha})\)。

注意根据这个定义，size\((A\circ B)=\text{size}(B)\)。这是一个关键性质，我们将在后面使用。

**命题 2.14**：在定义 2.13 的情况下，我们有 \(f_{A\circ B}=\widehat{f}_{A}\circ f_{B}\)。证明 这更正式地证明了我们在定义 2.13 中解释的直觉。

我们沿用定义 2.13 的记号。

给定一个索引 \(0\leq k\leq\alpha\)，令 \(\delta_{k}\in\mathbb{N}^{\times(\alpha+1)}\) 表示在所有位置为零，除了第 \(k\) 个位置为 1 的坐标。具体来说，

\[\delta_{0} =\underbrace{(1,0,0,...,0)}_{\alpha+1}\] \[\delta_{1} =\underbrace{(0,1,0,...,0)}_{\alpha+1}\] \[\delta_{\alpha} =\underbrace{(0,0,0,...,1)}_{\alpha+1}\]

关于扩展布局 \(A\) 的同构，我们有

\[\hat{t}:{\mathbb{N}}\cong[0,M_{0})\times[0,M_{1})\times...\times[0,M_{\alpha-1} )\times{\mathbb{N}}\]

因为 \(B=(N):(r)\)，我们有

$$\begin{aligned}
f_B(k) &= k \cdot r \\
&= M_{0} \cdot M_{1} \cdot \ldots \cdot M_{i - 1} \cdot k \cdot c \\
\end{aligned}$$

其中 \(k\in[0,N-1]\)。

首先考虑 \(0\leq i<\alpha\) 的情况。

如果 \(N\leq\frac{M_{i}}{c}\)，即 \(N\cdot c\leq M_{i}\)，那么对于所有 \(k\in[0,N-1]\)，我们必须有 \(k\cdot c<M_{i}\)。由于扩展布局 \(A\) 的同构，我们有

\[f_{B}(k)\mapsto\delta_{i}\cdot k\cdot c\]

那么我们有

$$\begin{aligned}
\left(\widehat{f}_A \circ f_B \right)(k) &= \widehat{f}_A \left(f_B\left(k\right)\right) \\
&= \widehat{f}_A \left(\delta_{i} \cdot k \cdot c\right) \\
&= k \cdot c \cdot d_{i} \\
\end{aligned}$$

根据定义 2.13，我们有

\[f_{A\circ B}(k) =k\cdot c\cdot d_{i}\]

因此，\(f_{A\circ B}=\hat{f}_{A}\circ f_{B}\)。

否则，如果 \(N>\frac{M_{i}}{c}\)，即 \(N=\frac{M_{i}}{c}\cdot...\cdot M_{j-1}\cdot c^{\prime}\)。由于扩展布局 \(A\) 的同构，根据定义，我们有

$$\begin{aligned}
f_B(k) &\mapsto \left(f_B(k) \mod M_{0}, \left\lfloor \frac{f_B(k)}{M_{0}} \right\rfloor \mod M_{1}, \left\lfloor \frac{f_B(k)}{M_{0} \cdot M_{1}} \right\rfloor \mod M_{2}, \ldots, \left\lfloor \frac{f_B(k)}{M_{0} \cdot M_{1} \cdot \ldots \cdot M_{i - 1}} \right\rfloor \mod M_{i}, \ldots, \left\lfloor \frac{f_B(k)}{M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 2}} \right\rfloor \mod M_{\alpha - 1}, \left\lfloor \frac{f_B(k)}{M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1}} \right\rfloor \right) \\
&= \left(0, 0, \ldots, \left(k \cdot c\right) \mod M_{i}, \left\lfloor \frac{k \cdot c}{M_{i}} \right\rfloor \mod M_{i + 1}, \ldots, \left\lfloor \frac{k \cdot c}{M_{i} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor \mod M_{j}, \ldots, \left\lfloor \frac{k \cdot c}{M_{i} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 2}} \right\rfloor \mod M_{\alpha - 1}, \left\lfloor \frac{k \cdot c}{M_{i} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 1}} \right\rfloor \right) \\
&= \left(0, 0, \ldots, \left( k \mod \frac{M_{i}}{c} \right) \cdot c, \left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor \mod M_{i + 1}, \ldots, \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor \mod M_{j}, \ldots, \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 2}} \right\rfloor \mod M_{\alpha - 1}, \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{\alpha - 1}} \right\rfloor \right) \\
\end{aligned}$$

注意这里我们使用了性质：如果 \(c\) 整除 \(M_{i}\)，则 \((k\cdot c)\equiv\left(k\bmod\frac{M_{i}}{c}\right)\cdot c\pmod{M_{i}}\)。

要看到这一点，假设 \(k\cdot c=p\cdot M_{i}+r\)，其中 \(0\leq r<M_{i}\)。那么我们有 \(k=\frac{p\cdot M_{i}+r}{c}=p\cdot\frac{M_{i}}{c}+\frac{r}{c}\)。因为 \(c\) 整除 \(M_{i}\)，\(\frac{r}{c}\) 必须是整数。因此，我们有 \((k\cdot c)\mod M_{i}=r\)，且 \(\left(k\mod\frac{M_{i}}{c}\right)\cdot c=\frac{r}{c}\cdot c=r\)。所以，\((k\cdot c)\mod M_{i}=\left(k\mod\frac{M_{i}}{c}\right)\cdot c\)。

此外，因为 \(0\leq k<N\)，我们有 \(0\leq k\cdot c<N\cdot c=M_{i}\cdot...\cdot M_{j-1}\cdot c^{\prime}\)，其中 \(1\leq c^{\prime}<M_{j}\)。因此 \(0\leq k\cdot c<M_{i}\cdot...\cdot M_{j-1}\cdot M_{j}\)。

当 \(c^{\prime}>1\) 时，我们有

$$\begin{aligned}
{\left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, M_{j}} & {= \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, c^{\prime}}
\end{aligned}$$

因此，我们有

$$\begin{aligned}
{f_{B}(k)} & {\mapsto\left( 0,0,\ldots,\left( k\quad{mod}\,\,\frac{M_{i}}{c} \right) \cdot c,\left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor\quad{mod}\,\, M_{i + 1},\ldots,\left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, c^{\prime},0,0,\ldots,0 \right)}
\end{aligned}$$

因此，我们有

$$\begin{aligned}
{\left( {\hat{f}}_{A} \circ f_{B} \right)(k)} & {= {\hat{f}}_{A}\left( f_{B}(k) \right)} \\
 & {= {\hat{f}}_{A}\left( 0,0,\ldots,\left( k\quad{mod}\,\,\frac{M_{i}}{c} \right) \cdot c,\left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor\quad{mod}\,\, M_{i + 1},\ldots,\left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, c^{\prime},0,0,\ldots,0 \right)} \\
 & {= 0 \cdot d_{0} + 0 \cdot d_{1} + \ldots + \left( k\quad{mod}\,\,\frac{M_{i}}{c} \right) \cdot c \cdot d_{i} + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor\quad{mod}\,\, M_{i + 1} \right) \cdot d_{i + 1} + \ldots + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, c^{\prime} \right) \cdot d_{j} + 0 \cdot d_{j + 1} + \ldots + 0 \cdot d_{\alpha}} \\
 & {= \left( k\quad{mod}\,\,\frac{M_{i}}{c} \right) \cdot c \cdot d_{i} + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor\quad{mod}\,\, M_{i + 1} \right) \cdot d_{i + 1} + \ldots + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, c^{\prime} \right) \cdot d_{j}}
\end{aligned}$$

根据定义 2.13，我们有

$$\begin{aligned}
{f_{A \circ B}(k)} & {= \left( k\quad{mod}\,\,\frac{M_{i}}{c} \right) \cdot c \cdot d_{i} + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor\quad{mod}\,\, M_{i + 1} \right) \cdot d_{i + 1} + \ldots + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 1}} \right\rfloor\quad{mod}\,\, c^{\prime} \right) \cdot d_{j}}
\end{aligned}$$

因此，\( f_{A\circ B} = \hat{f}_{A}\circ f_{B} \)。

当 \( c^{\prime}=1 \) 时，我们有 \( 0\leq k\cdot c<N\cdot c=M_{i}\cdot...\cdot M_{j-1}\cdot c^{\prime}=M_{i}\cdot... \cdot M_{j-1} \)。

因此，我们有

\[\left\lfloor\frac{k}{\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{j-1}}\right \rfloor = \left\lfloor\frac{k\cdot c}{M_{i}\cdot M_{i+1}\cdot...\cdot M_{j-1}}\right\rfloor = 0\]

\[\left\lfloor\frac{k}{\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{j-1}}\right \rfloor\mod M_{j} = 0\]

因此，我们有

\[f_{B}(k) \mapsto \left(0,0,...,\left(k\mod\frac{M_{i}}{c}\right)\cdot c,\left \lfloor\frac{k}{\frac{M_{i}}{c}}\right\rfloor\mod M_{i+1},...,\left\lfloor\frac{ k}{\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{j-2}}\right\rfloor\mod M_{j-1},0,0 ,...,0\right)\]

那么我们有

\[\left(\hat{f}_{A}\circ f_{B}\right)(k) = \hat{f}_{A}\left(f_{B}(k)\right)\] \[= \hat{f}_{A}\left(0,0,...,\left(k\mod\frac{M_{i}}{c}\right)\cdot c, \left\lfloor\frac{k}{\frac{M_{i}}{c}}\right\rfloor\mod M_{i+1},...,\left\lfloor\frac {k}{\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{j-1}}\right\rfloor\right)\] \[= 0\cdot d_{0}+0\cdot d_{1}+...+\left(k\mod\frac{M_{i}}{c}\right) \cdot c\cdot d_{i}+\left(\left\lfloor\frac{k}{\frac{M_{i}}{c}}\right\rfloor\mod M _{i+1}\right)\cdot d_{i+1}+...+\left(\left\lfloor\frac{k}{\frac{M_{i}}{c}\cdot M _{i+1}\cdot...\cdot M_{j-1}}\right\rfloor\right)\] \[= \left(k\mod\frac{M_{i}}{c}\right)\cdot c\cdot d_{i}+\left(\left \lfloor\frac{k}{\frac{M_{i}}{c}}\right\rfloor\mod M_{i+1}\right)\cdot d_{i+1}+... +\left(\left\lfloor\frac{k}{\frac{M_{i}}{c}\cdot M_{i+1}\cdot...\cdot M_{j-1}} \right\rfloor\right)\cdot d_{j}\]

根据定义 2.13，我们有

$$\begin{matrix}
{f_{A \circ B}(k)} & {= \left( k\quad{mod}\,\,\frac{M_{i}}{c} \right) \cdot c \cdot d_{i} + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c}} \right\rfloor\quad{mod}\,\, M_{i + 1} \right) \cdot d_{i + 1} + \ldots + \left( \left\lfloor \frac{k}{\frac{M_{i}}{c} \cdot M_{i + 1} \cdot \ldots \cdot M_{j - 2}} \right\rfloor\quad{mod}\,\, M_{j - 1} \right) \cdot d_{j - 1}}
\end{matrix}$$

因此，\( f_{A\circ B} = \hat{f}_{A}\circ f_{B} \)。

然后考虑 \( i=\alpha \) 的情况。

\[f_{B}(k) = k\cdot r\] \[= k\cdot M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\cdot c\]

其中 \( k\in[0,N-1] \)。

由于扩展布局 \(A\) 的同构，我们有

\[f_{B}(k)\ \mapsto\delta_{\alpha}\cdot k\cdot c\]

那么我们有

\[\left(\widehat{f}_{A}\circ f_{B}\right)(k) = \widehat{f}_{A}\left(f_{B}(k)\right)\] \[= \widehat{f}_{A}\left(\delta_{\alpha}\cdot k\cdot c\right)\] \[= k\cdot c\cdot d_{\alpha}\]

根据定义 2.13，我们有

\[f_{A\circ B}(k)\ \ =k\cdot c\cdot d_{\alpha}\]

因此，\(f_{A\circ B}=\widehat{f}_{A}\circ f_{B}\)。

综上所述，对于定义 2.13 中的所有情况，我们有 \(f_{A\circ B}=\widehat{f}_{A}\circ f_{B}\)。

证明完毕。

有人可能会问为什么复合可接纳性的第二个条件是必要的。如果我们没有它，\(c^{\prime}\) 可以是分数，我们仍然可以定义 \(A\circ B\) 为

$$\begin{array}{r}
{A \circ B = \left\{ \begin{matrix}
{\left( \frac{M_{i}}{c},M_{i + 1},\ldots,M_{j - 1},\left\lceil c^{\prime} \right\rceil \right):\left( cd_{i},d_{i + 1},\ldots,d_{j - 1},d_{j} \right)} & {\text{if~}c^{\prime} > 1} \\
{\left( \frac{M_{i}}{c},M_{i + 1},\ldots,M_{j - 1} \right):\left( cd_{i},d_{i + 1},\ldots,d_{j - 1} \right)} & {\text{if~}c^{\prime} = 1}
\end{matrix} \right.}
\end{array}$$

不难证明，当 \(B\) 的长度为 1 时，对于 \(f_{B}\) 的定义域，我们仍然有 \(f_{A\circ B}=\widehat{f}_{A}\circ f_{B}\)。

然而，在这种情况下，关键性质 \(\operatorname{size}(A\circ B)=\operatorname{size}(B)\) 将不成立。正如我们稍后将看到的，如果没有这个性质，当 \(B\) 是多模式的时候，即 \(B\) 的长度大于 1 时，\(f_{A\circ B}=\widehat{f}_{A}\circ f_{B}\) 就不能成立。

**定义 2.16**：**定义区间**

在定义 2.12 的情况下，其中布局 \(B\) 长度为 1，令 \(f_{B}:[0,N)\to\mathbb{N}\) 为布局函数，令 \(I=[r,r(N-1)]\) 为像 \(f_{B}([1,N))\) 的凸闭包给出的区间。令 \(M^{\prime}=M_{0}\cdot M_{1}\cdot...\cdot M_{\alpha-1}\) 且 \(J=I\cap[1,M^{\prime})\)（因此如果 \(\alpha=0\)，则 \(J=\emptyset\)）。那么 \(\{\mathbf{S},B\}\) 的定义区间为 \(J\)。

**定义 2.17**：**复合 - 一般情况**

令 \(\mathbf{S}=(M_{0},M_{1},...,M_{\alpha})\) 为形状元组，令 \(B=(N_{0},N_{1},...,N_{\beta}):(r_{0},r_{1},...,r_{\beta})\) 为一个布局，令 \(B_{k}=(N_{k}):(r_{k})\)，其中 \(0\leq k\leq\beta\)。那么如果满足以下条件，我们说对 \(\{\mathbf{S},B\}\) 对于复合是可接纳的：

1. 对于所有 \(0\leq k\leq\beta\)，对 \(\{\mathbf{S},B_{k}\}\) 在定义 2.12 的意义上对于复合是可接纳的。
2. 对 \(\{\mathbf{S},B_{k}\}_{0\leq k\leq\beta}\) 的定义区间是互不相交的。

在这种情况下，如果 \(\mathbf{D}=(d_{0},d_{1},...,d_{\alpha})\) 是一个步长元组且 \(A=\mathbf{S}:\mathbf{D}\)，那么我们定义复合 \(A\circ B\) 为连接的布局

\[A\circ B:=\left(A\circ B_{0},A\circ B_{1},...,A\circ B_{\beta}\right)\]

其中每个 \(A\circ B_{k}\) 如定义 2.13 中所定义。

**定理 2.18 复合 - 一般情况**

在定义 2.17 的情况下，我们有 \(f_{A\circ B}=\widehat{f}_{A}\circ f_{B}\)。

证明

根据定义 2.13，对于所有 \(0\leq k\leq\beta\)，\(\text{size}(A\circ B_{k})=\text{size}(B_{k})=N_{k}\)。我们对布局 \(A\circ B\) 或布局 \(B\) 有以下同构。

\[\iota:[0,N_{0}\cdot N_{1}\cdot...\cdot N_{\beta})\cong[0,N_{0})\times[0,N_{1}) \times...\times[0,N_{\beta})\]

给定任何 \(x\in[0,N_{0}\cdot N_{1}\cdot...\cdot N_{\beta})\)，由于同构 \(\iota\)，我们有

\[x\ \mapsto(x_{0},x_{1},...,x_{\beta})\]

根据引理 2.19，我们有

$$\begin{aligned}
\widehat{f}_A \circ f_B(x) &= \widehat{f}_A \left(f_B(x)\right) \\
&= \widehat{f}_A \left(f_{B_{0}}(x_{0}) + f_{B_{1}}(x_{1}) + \ldots + f_{B_{\beta}}(x_{\beta})\right) \\
\end{aligned}$$

根据定义 2.17、引理 2.19 和定义 2.13，我们有

$$\begin{aligned}
f_{A \circ B}(x) &= f_{A \circ B_{0}}(x_{0}) + f_{A \circ B_{1}}(x_{1}) + \ldots + f_{A \circ B_{\beta}}(x_{\beta}) \\
&= \widehat{f}_A \circ f_{B_{0}}(x_{0}) + \widehat{f}_A \circ f_{B_{1}}(x_{1}) + \ldots + \widehat{f}_A \circ f_{B_{\beta}}(x_{\beta}) \\
&= \widehat{f}_A \left(f_{B_{0}}(x_{0})\right) + \widehat{f}_A \left(f_{B_{1}}(x_{1})\right) + \ldots + \widehat{f}_A \left(f_{B_{\beta}}(x_{\beta})\right) \\
\end{aligned}$$

通常，我们没有 \(\hat{f}_{A}\left(x_{A,0}+x_{A,1}+...+x_{A,\beta}\right)=\hat{f}_{A}\left(x_{A,0 }\right)+\hat{f}_{A}\left(x_{A,1}\right)+...+\hat{f}_{A}\left(x_{A,\beta}\right)\)，因为布局函数 \(\hat{f}_{A}\) 不是线性的。例如，假设 \(A=(2,3):(1,4)\)，我们有 \(\hat{f}_{A}(1)=1\) 和 \(\hat{f}_{A}(3)=5\)。 \(\hat{f}_{A}(3)=\hat{f}_{A}(1+1+1)\neq\hat{f}_{A}(1)+\hat{f}_{A}(1)+\hat{f}_{A} (1)\)。

然而，在一些特殊情况下，上述等式成立。例如，为简单起见，假设 \(\beta=\alpha\)，如果我们有

$$\begin{aligned}
x_{A, 0} &\in [0, M_{0}) \\
x_{A, 1} &\in \{0, 1 \cdot M_{0}, 2 \cdot M_{0}, \ldots, \infty \cdot M_{0}\} \cap [0, M_{1}) \\
x_{A, 2} &\in \{0, 1 \cdot M_{0} \cdot M_{1}, 2 \cdot M_{0} \cdot M_{1}, \ldots, \infty  \cdot M_{0} \cdot M_{1}\} \cap [0, M_{2}) \\
&\vdots \\
x_{A, \alpha} &\in \{0, 1 \cdot M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1}, 2 \cdot M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1}, \ldots, \infty  \cdot M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1}\} \cap [0, M_{\alpha}) \\
\end{aligned}$$

根据定义，

\[\hat{f}_{A}(x)\ =\left(x\mod M_{0}\right)\cdot d_{0}+\left(\lfloor\frac{x}{M_{0 }}\rfloor\mod M_{1}\right)\cdot d_{1}+...+\left(\lfloor\frac{x}{M_{0}\cdot M_{1 }\cdot...\cdot M_{\alpha-1}}\rfloor\mod M_{\alpha}\right)\cdot d_{\alpha}\]

所以，在我们的情况下，我们有

$$\begin{aligned}
\widehat{f}_A \left(x_{A, 0} + x_{A, 1} + \ldots + x_{A, \beta}\right)
&= \left( \left( x_{A, 0} + x_{A, 1} + \ldots + x_{A, \beta} \right) \mod M_{0} \right) \cdot d_{0} + \left( \left\lfloor \frac{x_{A, 0} + x_{A, 1} + \ldots + x_{A, \beta}}{M_{0}} \right\rfloor \mod M_{1} \right) \cdot d_{1} + \ldots + \left( \left\lfloor \frac{x_{A, 0} + x_{A, 1} + \ldots + x_{A, \beta}}{M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1}} \right\rfloor \mod M_{\alpha} \right) \cdot d_{\alpha} \\
&= \left( x_{A, 0} \mod M_{0} \right) \cdot d_{0} + \left( \left\lfloor \frac{x_{A, 1}}{M_{0}} \right\rfloor \mod M_{1} \right) \cdot d_{1} + \ldots + \left( \left\lfloor \frac{x_{A, \beta}}{M_{0} \cdot M_{1} \cdot \ldots \cdot M_{\alpha - 1}} \right\rfloor \mod M_{\alpha} \right) \cdot d_{\alpha} \\
&= \widehat{f}_A \left(x_{A, 0}\right) + \widehat{f}_A \left(x_{A, 1}\right) + \ldots + \widehat{f}_A \left(x_{A, \beta}\right) \\
\end{aligned}$$

复合可接纳性的第二个条件的思想，即对 \(\{\mathbf{S},B_{k}\}_{0\leq k\leq\beta}\) 的定义区间互不相交，正是如此。

因为 \(r_{k}=M_{0}\cdot M_{1}\cdot...\cdot M_{i_{k}-1}\cdot c\)，对于 \(x_{k}\in[0,N_{k})\)，我们有

$$\begin{aligned}
f_{B_{k}}(x_{k}) &\in [0, 1 \cdot r, 2 \cdot r, \ldots, (N_{k} - 1) \cdot r] \\
&= [0, M_{0} \cdot M_{1} \cdot \ldots \cdot M_{i_{k} - 1} \cdot c, 2 \cdot M_{0} \cdot M_{1} \cdot \ldots \cdot M_{i_{k} - 1} \cdot c, \ldots, (N_{k} - 1) \cdot M_{0} \cdot M_{1} \cdot \ldots \cdot M_{i_{k} - 1} \cdot c] \\
\end{aligned}$$

由于布局 \(A\) 的同构，我们有

\[f_{B_{k}}(x_{k}) \mapsto (x_{A,0},x_{A,1},...,x_{A,\alpha})\]

其中

\[x_{A,i}=\left\lfloor\frac{f_{B_{k}}(x_{k})}{M_{0}\cdot M_{1}\cdot...\cdot M_{i- 1}}\right\rfloor\mod M_{i}\]

那么我们必须有一些整数 \(p_{k}\) 和 \(q_{k}\)，\(p_{k}<q_{k}\)，使得当 \(i<p_{k}\) 或 \(i>q_{k}\) 时，\(x_{A,i}=0\)。

复合可接纳性的第二个条件确保对于所有 \(0\leq k\leq\beta\)，\([p_{k},q_{k}]\) 是互不相交的。因此，我们可以得到等式：

\[\widehat{f}_{A}\left(x_{A,0}+x_{A,1}+...+x_{A,\beta}\right)=\widehat{f}_{A} \left(x_{A,0}\right)+\widehat{f}_{A}\left(x_{A,1}\right)+...+\widehat{f}_{A} \left(x_{A,\beta}\right)\]

证明完毕。

回到关于命题 2.14 中受限情况下复合可接纳性的第二个条件为什么必要的讨论。

因为关键性质 \(\textrm{size}(A\circ B)=\textrm{size}(B)\) 将不成立，我们将有两个完全不同的同构分别用于布局 \(A\circ B\) 和布局 \(B\)。

**引理 2.19 布局的连接**

令 \(C=(C_{0},C_{1},...,C_{\gamma})\) 为一个连接的布局。令

\[\iota:[0,\textrm{size}(C))\cong[0,\textrm{size}(C_{0}))\times...\times[0, \textrm{size}(C_{\gamma}))\]

为通常的同构（如定义 2.3）。那么下图交换：

\[\begin{array}{ccc}[0,\textrm{size}(C))&\stackrel{{\iota}}{{\to }}&[0,\textrm{size}(C_{0}))\times...\times[0,\textrm{size}(C_{ \gamma}))\\ f_{C}\downarrow&&\downarrow(f_{C_{0}},...,f_{C_{\gamma}})\\ \mathbb{N}&\stackrel{{+}}{{\leftarrow}}&\mathbb{N}\times...\times \mathbb{N}\\ \end{array}\]

证明

如果 \(C_{0},...,C_{\gamma}\) 都是长度为 1 的布局，那么根据定义 2.3，这是显然的。

具体来说，假设对于所有 \(0\leq k\leq\gamma\)，\(C_{k}=(M_{k}):(d_{k})\)。连接的布局变为

\[C =(C_{0},C_{1},...,C_{\gamma})\] \[=(M_{0}:d_{0},M_{1}:d_{1},...,M_{\gamma}:d_{\gamma})\] \[=(M_{0},M_{1},...,M_{\gamma}):(d_{0},d_{1},...,d_{\gamma})\]

由于布局 \(C\) 的同构，我们有

\[x\ \mapsto(x_{0},x_{1},...,x_{\gamma})\]

那么根据定义，连接的布局函数为

\[f_{C}(x)\ =\ x_{0}\cdot d_{0}+x_{1}\cdot d_{1}+...+x_{\gamma}\cdot d_{\gamma}\]

对于每个长度为 1 的布局 \(C_{k}\)，根据定义，布局函数为

\[f_{C_{k}}(x_{k})\ =\ x_{k}\cdot d_{k}\]

因此，我们有

\[f_{C}(x)\ =\ f_{C_{0}}(x_{0})+f_{C_{1}}(x_{1})+...+f_{C_{\gamma}}(x_{\gamma})\]

在有些布局 \(C_{k}\) 不是长度为 1 的情况下，我们可以对每个子布局 \(C_{k}\) 应用相同的论证，结果通过归纳法得出。

具体来说，假设 \(C_{k}\) 不是长度为 1，且 \(C_{k}=(C_{k,0},C_{k,1},...,C_{k,\gamma_{k}})\)，其中 \(C_{k,0},...,C_{k,\gamma_{k}}\) 是长度为 1 的布局。基于我们上面已经证明的，我们有

\[f_{C_{k}}(x_{k})\ =\ f_{C_{k,0}}(x_{k,0})+f_{C_{k,1}}(x_{k,1})+...+f_{C_{k, \gamma_{k}}}(x_{k,\gamma_{k}})\]

其中

\[x_{k}\ \mapsto\left(x_{k,0},x_{k,1},...,x_{k,\gamma_{k}}\right)\]

假设布局 \(C\) 可以极大分解为长度为 1 的布局。

\[C =(C_{0},C_{1},...,C_{\gamma})\] \[=(C_{0,0},C_{0,1},...,C_{0,\gamma_{0}},C_{1,0},C_{1,1},...,C_{1, \gamma_{1}},...,C_{\gamma,0},C_{\gamma,1},...,C_{\gamma,\gamma_{\gamma}})\]

那么我们有

\[f_{C}(x)\ =\ f_{C_{0,0}}(x_{0,0})+f_{C_{0,1}}(x_{0,1})+...+f_{C_ {0,\gamma_{0}}}(x_{0,\gamma_{0}})+f_{C_{1,0}}(x_{1,0})+f_{C_{1,1}}(x_{1,1})+...+f _{C_{1,\gamma_{1}}}(x_{1,\gamma_{1}})+...\] \[=f_{C_{0}}(x_{0})+f_{C_{1}}(x_{1})+...+f_{C_{\gamma}}(x_{\gamma})\]

其中

\[x\ \ \mapsto(x_{0},x_{1},...,x_{\gamma})\]

证明完毕。

定义 2.21 CUTLASS 复合可接纳性 - 受限情况

CUTLASS 中受限情况的复合可接纳性限制更强。

令 \(S = (M_{0}, M_{1}, ..., M_{\alpha})\) 为形状元组，令 \(M = M_{0} \cdot M_{1} \cdot ... \cdot M_{\alpha}\)，令 \(B = (N) : (r)\) 为长度为 1 的布局。那么如果满足以下条件，我们说对 \(\{S, B\}\) 对于复合是可接纳的（或简称为可接纳）：

1. \(M\) 被 \(r\) 左可除。写作 \(\hat{M} = r \cdot \hat{M}^{\prime}\)。
2. 关于其诱导因式分解，\(\hat{M}^{\prime}\) 被 \(N\) 左可除。

注意第二个条件是左可除性，而不是定义 2.12 中的弱左可除性。

例如，假设 \(A = (8, 6, 8) : (1, 16, 108)\) 且 \(B = (8) : (4)\)。根据定义 2.12，\(A \circ B = (2, 4) : (4, 16)\)。然而，如果我们在 CUTLASS 中运行 \(A\) 和 \(B\) 的复合，将会遇到错误，因为 CUTLASS 要求第二个条件是左可除性。

更具体地说，在 CUTLASS 的布局代数实现中，我们有

CUTLASS 强制执行此限制的原因是逻辑除法操作。没有这个限制，在某些情况下逻辑除法操作将无法定义。

**按模式复合**

在某些情况下，我们希望分别对布局的每个模式执行复合。

令布局 \(A\) 为布局的连接 \(A = (A_{0}, A_{1}, ..., A_{\alpha})\)，且 \(B\) 为布局的平铺 \(B = \langle B_{0}, B_{1}, ..., B_{\alpha} \rangle\)。注意 \(B\)，一个平铺器，只是一个布局元组，而不是连接的布局。一个平铺器可能包含一个或多个布局。那么我们定义按模式复合 \(A \circ B\) 为布局

\[A \circ B := (A_{0} \circ B_{0}, A_{1} \circ B_{1}, ..., A_{\alpha} \circ B_{\alpha})\]

在某些情况下，我们也有布局 \(A\) 为布局的连接 \(A = (A_{0}, A_{1}, ..., A_{\alpha})\)，且 \(B\) 为布局的平铺 \(B = \langle B_{0}, B_{1}, ..., B_{\beta} \rangle\)，其中 \(\beta \geq \alpha\)。在这种情况下，我们定义按模式复合 \(A \circ B\) 为布局

\[A \circ B := (A_{0} \circ B_{0}, A_{1} \circ B_{1}, ..., A_{\alpha} \circ B_{\alpha})\]

**非整数布局复合**

类似于非整数布局补集，非整数布局复合可以通过合并布局使其变为整数布局来推导。上述所有整数布局复合的性质都可以使用。

**复合的含义**

复合操作通常用于从布局中选择子布局。如果执行按模式复合，子布局的每个模式都可以是跨步的。

### 逻辑除法

#### 定义 2.22 逻辑除法

令 \(A=\mathbf{S}:\mathbf{D}\) 和 \(B\) 为布局，令 \(M\) 为 \(A\) 的大小。假设对 \(\{B,M\}\) 和 \(\{\mathbf{S},B\}\) 是可接纳的（分别对于补集和复合）。那么我们定义逻辑除法 \(A/B\) 为布局

\[A/B:=A\circ(B,\text{complement}(B,M))\]

注意这里复合可接纳性的条件遵循定义 2.21 而不是定义 2.12。

隐式地，定义 2.22 中使用了引理 2.23。

#### 引理 2.23 逻辑除法的含义

假设 \(A=\mathbf{S}:\mathbf{D}\)，\(M=\text{size}(A)\)，且 \(B\) 如定义 2.22 中所述。那么 \(\{\mathbf{S},(B,\text{complement}(B,M))\}\) 对于复合是可接纳的。

证明

我们记 \(A=\mathbf{S}:\mathbf{D}=(M_{0},M_{1},...,M_{\alpha}):(d_{0},d_{1},...,d_{\alpha})\)，且 \(B=(N_{0},N_{1},...,N_{\beta}):(r_{0},r_{1},...,r_{\beta})\)。令

\[\varphi:[0,\beta]\stackrel{{\cong}}{{\rightarrow}}[0,\beta]\]

为一个自同构，使得 \(B^{\varphi}\ :=\ (N_{\varphi(0)},N_{\varphi(1)},...,N_{\varphi(\beta)}):(r_{ \varphi(0)},r_{\varphi(1)},...,r_{\varphi(\beta)})\) 是排序的。

那么根据定义 2.6，我们有

\[B^{\prime} =\text{complement}(B,M)\] \[=\left(r_{\varphi(0)},\frac{r_{\varphi(1)}}{N_{\varphi(0)}r_{ \varphi(0)}},\frac{r_{\varphi(2)}}{N_{\varphi(1)}r_{\varphi(1)}},...,\frac{r_{ \varphi(\beta)}}{N_{\varphi(\beta-1)}r_{\varphi(\beta-1)}},\frac{M}{N_{\varphi (\beta)}r_{\varphi(\beta)}}\right):(1,N_{\varphi(0)}r_{\varphi(0)},N_{\varphi(1 )}r_{\varphi(1)},...,N_{\varphi(\beta-1)}r_{\varphi(\beta-1)}.\]

现在我们记 \(B^{\prime}\) 的每个模式为

\[B^{\prime}_{k} =\begin{cases}\binom{r_{\varphi(0)}}{r_{\varphi(k)}}:(1)&\text{如果 }k=0\\ \left(\frac{N_{\varphi(k-1)}r_{\varphi(k-1)}}{M}\right):(N_{\varphi(k-1)}r_{ \varphi(k-1)})&\text{如果 }1\leq k\leq\beta\\ \left(\frac{N_{\varphi(\beta)}r_{\varphi(\beta)}}{M}\right):(N_{\varphi(\beta )}r_{\varphi(\beta)})&\text{如果 }k=\beta+1\end{cases}\]

对于 \(k\in[0,\beta+1]\)。

因为对 \(\{\mathbf{S},B\}\) 对于复合是可接纳的，对于 \(B\) 中的每个模式，\(B_{k}=(N_{k}):(r_{k})\)，其中 \(k\in[0,\beta]\)，根据定义 2.17，对 \(\{\mathbf{S},B_{k}\}\) 对于复合是可接纳的。因此，根据定义 2.12，\(M\) 被 \(r_{k}\) 左可除，且商 \(\frac{M}{r_{k}}\) 被 \(N_{k}\) 左可除（而不是弱左可除）对于所有 \(k\in[0,\beta]\)。

显然 \(M\) 被 \(1\) 左可除。让我们看看对于所有 \(k\in[1,\beta+1]\)，\(M\) 是否也被 \(N_{\varphi(k-1)}r_{\varphi(k-1)}\) 左可除。

假设 \(\varphi(k-1)=h\)，因为 \(M\) 被 \(r_{h}\) 左可除，我们有

\[r_{\varphi(k-1)} =r_{h}\] \[=M_{0}\cdot M_{1}\cdot...\cdot M_{i_{k-1}-1}\cdot c_{k-1}\]

其中 \(c_{k}\) 整除 \(M_{i_{k}}\)。

\[N_{\varphi(k-1)} =N_{h}\] \[=\tfrac{M_{i_{k}}}{c_{k-1}}\cdot M_{i_{k-1}+1}\cdot...\cdot M_{j_{k -1}-1}\cdot c^{\prime}_{k-1}\]

其中 \(c^{\prime}_{k-1}\) 整除 \(M_{j_{k-1}}\)。

因此，\(M\) 也被 \(N_{\varphi(k-1)}r_{\varphi(k-1)}\) 左可除，因为

\[N_{\varphi(k-1)}r_{\varphi(k-1)} =M_{0}\cdot M_{1}\cdot...\cdot M_{i_{k-1}-1}\cdot c_{k-1}\cdot \tfrac{M_{i_{k}}}{c_{k-1}}\cdot M_{i_{k}+1}\cdot...\cdot M_{j_{k-1}-1}\cdot c^{ \prime}_{k-1}\] \[=M_{0}\cdot M_{1}\cdot...\cdot M_{j_{k-1}-1}\cdot c_{k-1}\]

其中 \(c^{\prime}_{k-1}\) 整除 \(M_{j_{k-1}}\)。

接下来，我们将证明对于所有 \(k\in[1,\beta]\)，\(M\) 被 \(\tfrac{r_{\varphi(k)}}{N_{\varphi(k-1)}r_{\varphi(k-1)}}\) 左可除。

\[r_{\varphi(k)} =M_{0}\cdot M_{1}\cdot...\cdot M_{i_{k}-1}\cdot c_{k}\]

因为 \(r_{\varphi(k)}\geq N_{\varphi(k-1)}r_{\varphi(k-1)}\)，我们必须有 \(i_{k}\geq j_{k-1}\)。因此，

\[\tfrac{r_{\varphi(k)}}{N_{\varphi(k-1)}r_{\varphi(k-1)}} =\tfrac{M_{0}\cdot M_{1}\cdot...\cdot M_{i_{k}-1}\cdot c_{k}}{M_{0 }\cdot M_{1}\cdot...\cdot M_{j_{k-1}-1}\cdot c^{\prime}_{k-1}}\] \[=\tfrac{M_{j_{k-1}}\cdot M_{j_{k-1}+1}\cdot...\cdot M_{i_{k}-1} \cdot c_{k}}{c^{\prime}_{k-1}}\] \[=\tfrac{M_{j_{k-1}}}{c^{\prime}_{k-1}}\cdot M_{j_{k-1}+1}\cdot.. .\cdot M_{i_{k}-1}\cdot c_{k}\]

因此，对于所有 \(k\in[1,\beta]\)，\(M\) 被 \(\tfrac{r_{\varphi(k)}}{N_{\varphi(k-1)}r_{\varphi(k-1)}}\) 左可除。

显然 \(M\) 被 \(\tfrac{M}{N_{\varphi(\beta)}r_{\varphi(\beta)}}\) 左可除。

因此，对于所有 \(k\in[0,\beta+1]\)，对 \(\{\mathbf{S},B^{\prime}_{k}\}\) 对于复合是可接纳的。

根据定义 2.17，为了证明 \(\{\mathbf{S},(B,\text{complement}(B,M))\}\) 对于复合是可接纳的，我们还需要证明对 \(\{\mathbf{S},B_{k}\}_{0\leq k\leq\beta}\) 和 \(\{\mathbf{S},B^{\prime}_{k}\}_{0\leq k\leq\beta+1}\) 的定义区间互不相交。

根据命题 2.7，连接的布局 \((B,\text{complement}(B,M))\) 自动满足不相交的论证。

因此，\(\{\mathbf{S},(B,\text{complement}(B,M))\}\) 对于复合是可接纳的。

证明完毕。

注意在定义 2.22 中，如果复合可接纳性的条件遵循定义 2.12，我们上面的证明将无效。这就是为什么 CUTLASS 强制执行复合可接纳性的条件遵循定义 2.21。

#### 按模式逻辑除法

在某些情况下，我们希望分别对布局的每个模式执行逻辑除法。

令布局 \(A\) 为布局的连接 \(A=(A_{0},A_{1},...,A_{\alpha})\)，且 \(B\) 为布局的平铺 \(B=\langle B_{0},B_{1},...,B_{\alpha}\rangle\)。注意 \(B\) 只是一个布局元组，而不是连接的布局。那么我们定义按模式逻辑除法 \(A/B\) 为布局

\[A/B :=(A_{0}/B_{0},A_{1}/B_{1},...,A_{\alpha}/B_{\alpha})\] \[=(A_{0}\circ(B_{0},\text{complement}(B_{0},M)),A_{1}\circ(B_{1}, \text{complement}(B_{1},M)),...,A_{\alpha}\circ(B_{\alpha},\text{complement}(B_{0 },M)),(A_{1}\circ B_{1},A_{1}\circ\text{complement}(B_{1},M)),...,(A_{\alpha} \circ B_{\alpha},A_{\alpha}\circ\text{complement}(B_{1},M)).\]

在某些情况下，我们也有 \(A\) 为布局的连接 \(A=(A_{0},A_{1},...,A_{\alpha})\)，且 \(B\) 为布局的平铺 \(B=\langle B_{0},B_{1},...,B_{\beta}\rangle\)，其中 \(\alpha\geq\beta\)。在这种情况下，我们定义按模式逻辑除法 \(A/B\) 为布局

\[A/B :=(A_{0}/B_{0},A_{1}/B_{1},...,A_{\beta}/B_{\beta},A_{\beta+1},.. .,A_{\alpha})\] \[=(A_{0}\circ(B_{0},\text{complement}(B_{0},M)),A_{1}\circ(B_{1}, \text{complement}(B_{1},M)),...,A_{\beta}\circ(B_{\beta},\text{complement}(B_{0 },M)),(A_{1}\circ B_{1},A_{1}\circ\text{complement}(B_{1},M)),...,(A_{\beta} \circ B_{\beta},A_{\beta}\circ\text{complement}(B_{1},M)).\]

#### 逻辑除法变体

在按模式逻辑除法中，选择多维平铺不方便。给定逻辑除法 \(A/B\)，

$$\begin{matrix}
{A/B} & {= \left( \left( A_{0} \circ B_{0},A_{0} \circ \text{complement}(B_{0},M) \right),\left( A_{1} \circ B_{1},A_{1} \circ \text{complement}(B_{1},M) \right),\ldots,\left( A_{\beta} \circ B_{\beta},A_{\beta} \circ \text{complement}(B_{\beta},M) \right),A_{\beta + 1},\ldots,A_{\alpha} \right)}
\end{matrix}$$

我们希望通过索引从 \(A/B\) 中迭代每个多维子布局  $\left( A_{0} \circ B_{0},A_{1} \circ B_{1},\ldots,A_{\beta} \circ B_{\beta} \right)$。

因此，通过重新排列布局 \(A/B\) 来引入zipped division：

$$\begin{matrix}
{\text{zipped\_division}(A,B)} & {= \left( \left( A_{0} \circ B_{0},A_{1} \circ B_{1},\ldots,A_{\beta} \circ B_{\beta} \right),\left( A_{0} \circ \text{complement}(B_{0},M),A_{1} \circ \text{complement}(B_{1},M),\ldots,A_{\beta} \circ \text{complement}(B_{\beta},M) \right),A_{\beta + 1},\ldots,A_{\alpha} \right)} \\
 & {= \left( A \circ B,\left( A_{0} \circ \text{complement}(B_{0},M),A_{1} \circ \text{complement}(B_{1},M),\ldots,A_{\beta} \circ \text{complement}(B_{\beta},M) \right),A_{\beta + 1},\ldots,A_{\alpha} \right)}
\end{matrix}$$


这样，我们可以通过第二个模式上的索引从 zipped\_division\((A,B)\) 中迭代每个多维子布局 \(A\circ B=(A_{0}\circ B_{0},A_{1}\circ B_{1},...,A_{\beta}\circ B_{\beta})\)。注意，因为 \(A_{0}\circ B_{0}\) 与 \(B_{0}\) 有相同的定义域，\(A_{1}\circ B_{1}\) 与 \(B_{1}\) 有相同的定义域，依此类推，\(A\circ B\) 的定义域或形状变得非常可预测，即 $(\text{shape}(B_{0}),\text{shape}(B_{1}),\ldots,\text{shape}(B_{\beta}))$。形状 $\left( A_{0} \circ \text{complement}(B_{0},M),A_{1} \circ \text{complement}(B_{1},M),\ldots,A_{\beta} \circ \text{complement}(B_{\beta},M) \right)$  不太可预测，但其一维大小是可预测的，即 $\left( \frac{M}{\text{size}(B_{0})},\frac{M}{\text{size}(B_{1})},\ldots,\frac{M}{\text{size}(B_{\beta})} \right)$。

CuTe 实现了四种逻辑除法变体，包括逻辑除法、拉链除法、平铺除法和展平除法。假设平铺器是两个布局的元组，逻辑除法变体定义如下：

**逻辑除法的含义**

因为我们已经证明了 \((B,\text{complement}(B,M))\) 是一个双射 \([0,M)\cong[0,M)\) 的布局，逻辑除法 \(A/B:=A\circ(B,\text{complement}(B,M))\)（其中 \(M\) 是 \(A\) 的大小）是一个与 \(A\) 有相同定义域并因此也有相同陪域的布局。这意味着原始布局 \(A\) 从坐标 \([0,M)\) 到整数的映射方式被重新排列或置换为一个新布局，即逻辑除法 \(A/B\)。

根据复合的定义，

$$\begin{aligned}
A / B
&:= A \circ \left(B, \text{complement}(B, M)\right) \\
&= \left( A \circ B, A \circ \text{complement}(B, M) \right) \\
\end{aligned}$$

\(A\circ B\) 是基于布局 \(B\) 从 \(A\) 中选择子布局（即平铺）的布局，而 \(A\circ\text{complement}(B,M)\) 则重复 \(A\circ B\) 布局以填满 \(A\) 的定义域和陪域。

逻辑除法告诉我们如何使用平铺器 \(B\) 从布局 \(A\) 中提取平铺，以及如何重复该平铺以填满 \(A\) 的定义域和陪域，或者如何使用索引从后续重复的布局中选择平铺。

### 逻辑乘积

**定义 4.1**：**逻辑乘积**

给定一个平铺器和一个布局，我们可以使用逻辑除法计算如何用平铺器重复布局以填满布局的定义域和陪域。类似地，给定一个平铺器和一个重复布局，我们可以使用逻辑乘积计算作为重复布局平铺的结果布局。

令 \(A\) 和 \(B\) 为布局，\(M=\text{size}(A)\text{cosize}(B)\)。假设对 \(\{A,M\}\) 和 \(\{\text{complement}(A,M),B\}\) 是可接纳的（分别对于补集和复合）。那么我们定义逻辑乘积 \(A\times B\) 为布局

\[A\times B:=(A,\text{complement}(A,M)\circ B)\]

布局 \(A\) 的补集找到一个具有正整数 \(M\) 的补集布局 complement\((A,M)\)，使得当两个布局连接时，例如 \((A,\text{complement}(A,M))\)，新布局是一个双射 \([0,M)\cong[0,M)\)。然而，我们想要创建的最终布局的陪域可能不需要是 \([0,M)\)。布局 \(B\) 的 \(\text{cosize}(B)\) 可能远大于 \(\text{size}(B)\)。因此，在使用 \(\text{complement}(A,M)\circ B\) 重复布局 \(A\) 之后，结果布局 \(A\times B\) 将填充到陪域 \(M\)，但可能是跨步的。此外，即使 \(\text{size}(B)=\text{cosize}(B)\)，我们也可以通过使用复合操作来置换 \(\text{complement}(A,M)\)，因为可能有多个布局的 \(\text{cosize}\) 是 \(\text{cosize}(B)\)。

因为 \(\text{complement}(A,M)\circ B\) 是 \(\text{complement}(A,M)\) 的子布局，布局连接 \((A,\text{complement}(A,M)\circ B)\) 仍然是一个有效的布局。

**按模式逻辑乘积**

在某些情况下，我们希望分别对布局的每个模式执行逻辑乘积。例如，如果我们想用二维平铺器平铺一个二维布局，我们希望分别对布局的每个模式执行逻辑乘积。

令布局 \(A\) 为布局的连接 \(A=(A_{0},A_{1},...,A_{\alpha})\)，且 \(B\) 为布局的平铺 \(B=\langle B_{0},B_{1},...,B_{\alpha}\rangle\)。注意 \(B\) 只是一个布局元组，而不是连接的布局。那么我们定义按模式逻辑乘积 \(A\times B\) 为布局

\[A\times B :=(A_{0}\times B_{0},A_{1}\times B_{1},...,A_{\alpha}\times B_{\alpha})\] \[=(A_{0},\text{complement}(A_{0},M)\circ B_{0},A_{1},\text{complement }(A_{1},M)\circ B_{1},...,A_{\alpha},\text{complement}(A_{\alpha},M)\circ B)\] \[=((A_{0},\text{complement}(A_{0},M)\circ B_{0})\,,(A_{1},\text{ complement}(A_{1},M)\circ B_{1})\,,...\,,(A_{\alpha},\text{complement}(A_{\alpha},M)\circ B).\]

**逻辑乘积变体**

类似于逻辑除法，也有逻辑乘积的变体，包括逻辑乘积、拉链乘积、平铺乘积和展平乘积，以方便平铺选择和迭代。假设平铺器是两个布局的元组，逻辑乘积变体定义如下：

**逻辑乘积的含义**

类似于逻辑除法，逻辑乘积 \(A\times B\) 告诉我们使用平铺器重复原始布局后布局是什么。

### 可表示为布局函数的排列

本节解释了如何以结构化的方式检索所有可表示为布局函数的排列。这很重要，因为 CUTLASS 和 CuTe 中使用的一些排列代数，例如 swizzle，似乎不能表达为布局函数。使用范畴论的基本语言来描述这个过程。

#### 定义 3.1 有序因式分解

我们定义有序因式分解的集合 \(\operatorname{ob}(\mathbf{Fact})\) 由所有表达式 \([p_{1}\,...\,p_{k}]\) 组成，其中 \(k\geq 0\) 且 \(p_{i}\) 是素数（不一定不同）。\(k=0\) 的情况对应于空因式分解，我们记作 \([\ ]\)。

例如，集合 \(\operatorname{ob}(\mathbf{Fact})\) 包括表达式如 \([\ ]\)，\([2]\)，\([3]\)，\([22]\)，\([23]\)，\([32]\)，\([232]\) 等。

#### 记号 3.3

令 \(k\) 表示由 \(k\) 个元素组成的集合 \(\{1,2,...,k\}\)。（如果 \(k=0\)，那么 \(\underline{0}=\emptyset\) 是空集。）

#### 定义 3.4 有序因式分解的范畴

我们定义有序因式分解的范畴 \(\mathbf{Fact}\) 如下：

1.  \(\operatorname{ob}(\mathbf{Fact})\) 是 \(\mathbf{Fact}\) 的对象集合。
2.  对于每个表达式 \(E=[p_{1}\,...\,p_{k}]\) 在 \(\operatorname{ob}(\mathbf{Fact})\) 中和每个有限集态射 \(\alpha\,:\,n\,\rightarrow\,k\)，我们有一个态射

\[E^{\alpha}=[p_{\alpha(1)}...p_{\alpha(n)}]\stackrel{{\alpha_{E}}}{{ \rightarrow}}E=[p_{1}...p_{k}]\]

在 \(\mathbf{Fact}\) 中。这定义了所有以 \(E\) 为陪域的态射的集合，并且遍历所有 \(E\) 从而定义了 \(\mathbf{Fact}\) 中所有态射的集合。

3.  态射的复合定义如下。假设我们有有限集态射 \(\alpha:n\stackrel{{}}{{\rightarrow}}k\) 和 \(\beta:m\stackrel{{}}{{\rightarrow}}n\)，以及表达式 \(E=[p_{1}...p_{k}]\)。写作
    \[E^{\alpha}=[p_{\alpha(1)}...p_{\alpha(n)}]=[q_{1}...q_{n}]\]

令 \(\gamma=\alpha\circ\beta:m\stackrel{{}}{{\rightarrow}}k\)。那么态射的复合

\[\alpha_{E}:E^{\alpha}=[p_{\alpha(1)}...p_{\alpha(n)}]\stackrel{{}}{ {\rightarrow}}E=[p_{1}...p_{k}]\]

\[\beta_{E^{\alpha}}:E^{\beta}=[q_{\beta(1)}...q_{\beta(m)}]\stackrel{{ }}{{\rightarrow}}E^{\alpha}=[q_{1}...q_{n}]\]

由 \(\gamma_{E}:E^{\gamma}\stackrel{{}}{{\rightarrow}}E\) 给出，其中我们使用了 \([q_{\beta(1)}...q_{\beta(m)}]=[p_{\gamma(1)}...p_{\gamma(m)}]\)。

很容易检查 \(\mathbf{Fact}\) 中态射的复合是结合的并且有单位元，这是范畴中复合必须满足的两个公理，所以定义 3.4 确实定义了一个范畴。

要理解为什么态射的复合是结合的，假设我们有有限集态射 \(\alpha:n\stackrel{{}}{{\rightarrow}}k\)，\(\beta:m\stackrel{{}}{{\rightarrow}}n\)，和 \(\gamma:\ l\stackrel{{}}{{\rightarrow}}m\)。那么我们有

\[\alpha\circ(\beta\circ\gamma)=(\alpha\circ\beta)\circ\gamma\]

要理解为什么态射的复合有单位元，假设对于每个 \(n\)，我们有一个有限集态射 \({\rm id}_{n}:n\stackrel{{}}{{\rightarrow}}n\)，使得对于所有 \(i\in n\)，\({\rm id}_{n}(i)=i\)。那么我们有

\[E^{{\rm id}_{n}}=[p_{{\rm id}_{n}(1)}...p_{{\rm id}_{n}(n)}]\stackrel{{ }}{{\rightarrow}}E=[p_{1}...p_{n}]\]

对于每个态射 \(\alpha:n\stackrel{{}}{{\rightarrow}}k\)，我们有

\[\alpha\circ{\rm id}_{n}={\rm id}_{k}\circ\alpha=\alpha\]

因此，\({\rm id}_{n}\) 是 \(n\) 的单位态射。

**记号 3.5**：令 \(\Sigma_{k}\) 表示 \(k\) 个字母上的对称群。给定一个元素 \(\varphi\in\Sigma_{k}\)，我们也用 \(\varphi\) 表示关联的 \(\overset{k}{\underset{-}{\smile}}\) 的自同构。

在数学中，群是一个集合，带有一个操作，该操作将集合的一个元素与集合的每对元素关联起来（就像每个二元操作一样），并满足以下约束：该操作是结合的，它有一个单位元，并且集合的每个元素都有一个逆元。

在这个意义上，对称群是一个集合的所有排列的集合，带有排列的复合操作（一个接一个地应用排列）。

假设 \(E=[222]\)。那么每个排列 \(\varphi\in\Sigma_{3}\) 定义了 \(\mathbf{Fact}\) 中的一个自同构 \(E^{\varphi}=E\to E\)。

假设 \(E=[232]\)。那么对换 \(\sigma=(13)\in\Sigma_{3}\) 定义了 \(\mathbf{Fact}\) 中的一个自同构 \(E^{\sigma}=E\to E\)。另一方面，对换 \(\tau=(12)\in\Sigma_{3}\) 定义了 \(\mathbf{Fact}\) 中的一个态射 \(E^{\tau}=[322]\to E=[232]\)。

**备注 3.7**：令 **FinSet** 表示有限集的范畴（或者更确切地说，一个骨架，对象由集合 \(n\) 给出，\(n\geq 0\)）。给定一个对象 \(k\in{\bf FinSet}\)，令 $\textbf{FinSet}^{/\underset{―}{k}}$  表示 [overcategory]，其对象是态射 \([\alpha:\,n\,\rightarrow\,k]\)，其态射是交换三角形。回想一下，这个范畴有一个终对象，由单位态射 \([{\rm id}_{k}]\) 给出。那么对于每个长度为 \(k\) 的表达式 \(E=[p_{1}\,...\,p_{k}]\)，我们有一个函子

$$\begin{array}{r}
{F_{E}:\textbf{FinSet}^{/\underset{―}{k}} \rightarrow \textbf{Fact}}
\end{array}$$

它将对象 $\lbrack\alpha:\underset{―}{n} \rightarrow \underset{―}{k}\rbrack$ 发送到表达式 $E^{\alpha}$，并将唯一态射 $\lbrack\alpha\rbrack\overset{}{\rightarrow}\lbrack\text{id}_{\underset{―}{k}}\rbrack$ 发送到 $\alpha_{E}:E^{\alpha}\overset{}{\rightarrow}E$。这个函子将 $\textbf{Fact}$ 中每个以 $E$ 为陪域的态射都包含在其像中。假设我们在  $\lbrack\alpha:\underset{―}{n} \rightarrow \underset{―}{k}\rbrack$ 中有一个态射对象 $\lbrack\beta:\underset{―}{m} \rightarrow \underset{―}{k}\rbrack$ 和另一个态射对象 $\textbf{FinSet}^{/\underset{―}{k}}$。那么 overcategory 的态射是一个交换三角形，其剩余的态射是 $\lbrack\gamma:\underset{―}{m} \rightarrow \underset{―}{n}\rbrack$，它将 $\lbrack\alpha:\underset{―}{n} \rightarrow \underset{―}{k}\rbrack$ 映射到 $\lbrack\beta:\underset{―}{m} \rightarrow \underset{―}{k}\rbrack$。

单位态射 $\lbrack\text{id}_{\underset{―}{k}}\rbrack$ 是 $\textbf{FinSet}^{/\underset{―}{k}}$ 的终对象，因为  $\textbf{FinSet}^{/\underset{―}{k}}$ 中每个其他态射对象都有一个唯一态射到 $\lbrack\text{id}_{\underset{―}{k}}\rbrack$。

在这种情况下，给定一个态射对象 \([\alpha:\,n\,\rightarrow\,k]\)，交换三角形有一个剩余态射 \([\gamma:\,k\,\rightarrow\,n]\)，并且我们必须证明这个交换三角形中的剩余态射是唯一的。

给定 \(k=\{1,2,...,k\}\)，单位态射将 \(i\in k\) 映射到 \(i\in k\)。给定一个态射 \([\alpha:\,n\,\rightarrow\,k]\)，其中不失一般性，我们假设对于所有 \(i\in[1,k]\)，\(\alpha(i)=i\)，剩余态射 \([\gamma:\,k\,\rightarrow\,n]\) 必须对于所有 \(i\in[1,k]\) 有 \(i=\alpha(i)\)，以便我们有一个交换三角形 \(i\in\,k\,\stackrel{{\gamma}}{{\rightarrow}}\,\stackrel{{ \alpha}}{{\rightarrow}}\,i\in\,k\)。否则，对于剩余态射 \([\gamma:\,k\,\rightarrow\,n]\)，如果存在一个 \(i\in\,k\) 使得 \(i\neq\alpha(i)\)，那么交换三角形将无效，因为 \(i\in\,k\,\stackrel{{\gamma}}{{\rightarrow}}\,\stackrel{{ \alpha}}{{\rightarrow}}\,j\in\,k\) 且 \(i\neq j\)。因此，交换三角形的态射是唯一的。

根据定义，令 \(C\) 和 \(D\) 为范畴。一个从 \(C\) 到 \(D\) 的函子 \(F\) 是一个映射，它

*   将 \(C\) 中的每个对象 \(X\) 关联到 \(D\) 中的一个对象 \(F(X)\)
*   将 \(C\) 中的每个态射 \(f:X\to Y\) 关联到 \(D\) 中的一个态射 \(F(f):F(X)\to F(Y)\)，使得
    *   对于 \(C\) 中的每个对象 \(X\)，\(F(\operatorname{id}_{X})=\operatorname{id}_{F(X)}\)，并且
    *   对于 \(C\) 中的所有态射 \(f:X\to Y\) 和 \(g:Y\to Z\)，有 \(F(g\circ f)=F(g)\circ F(f)\)。

在范畴 \(\mathsf{FinSet}^{k}\) 中，我们有 \(X=[\alpha:n\to k]\) 和 \(Y=[\gamma:m\to k]\)，\(Z=[\operatorname{id}_{k}]\)，且态射是交换三角形 \(f=[\alpha]\to[\gamma]\)，\(g=[\gamma]\to[\operatorname{id}_{k}]\)，和 \(g\circ f=[\alpha]\to[\operatorname{id}_{k}]\)。

在范畴 \(\mathsf{Fact}\) 中，通过函子 \(F_{E}\)，我们有 \(F_{E}(X)=E^{\alpha}=[p_{\alpha(1)}\dots p_{\alpha(n)}]\)，\(F_{E}(Y)=E^{\gamma}=[p_{\gamma(1)}\dots p_{\gamma(m)}]\)，\(F_{E}(Z)=E=[p_{1}\dots p_{k}]\)，且态射是 \(F_{E}(f)=\alpha_{E}:E^{\alpha}\to E^{\gamma}\)，\(F_{E}(g)=\gamma_{E}:E^{\gamma}\to E\)，和 \(F_{E}(g)\circ F_{E}(f)=\alpha_{E}:E^{\alpha}\to E\)。

**备注 3.8**：事实上，我们可以将 \(\mathsf{Fact}\) 本身识别为某个 overcategory（或者更确切地说，它的一个满子范畴）。即，令 \({\cal P}\) 表示无限素数集合 \(\{2,3,5...\}\)，令 \(\mathsf{Set}\) 为集合的范畴，并令 \(\mathsf{FinSet}^{{\cal P}}\) 为 \(\mathsf{Set}^{{\cal P}}\) 的满子范畴，其对象是那些态射 \(X\to{\cal P}\)，其中 \(X\) 是有限集。那么我们有一个范畴等价

\[\mathsf{Fact}\simeq\mathsf{FinSet}^{{\cal P}}\]

它将一个表达式 \(E=[p_{1}...p_{k}]\) 发送到态射 \(E_{\bullet}:k\to{\cal P}\)，由 \(i\mapsto p_{i}\) 给出。

在这个等价下，备注 3.7 中的函子 \(F_{E}\) 等同于函子

\[\mathsf{FinSet}^{k}\simeq\left(\mathsf{FinSet}^{{\cal P}}\right)^{E_{\bullet}} \to\mathsf{FinSet}^{{\cal P}}\]

它忘记到 \(E_{\bullet}\) 的映射。

要理解这一点，让我们考虑以下例子。

假设我们有一个来自 \(\mathsf{Fact}\) 的对象 \(E=[232]\)。那么我们有一个态射 \(E_{\bullet}:\underrightarrow{3}\to{\cal P}\)，由下式给出

\[i=1\quad\mapsto p_{1}=2\] \[i=2\quad\mapsto p_{2}=3\] \[i=3\quad\mapsto p_{3}=2\]

\(\mathsf{FinSet}^{{\cal P}}\) 中的每个态射对象都可以从 \(\mathsf{Fact}\) 的一个对象映射而来，因此我们有一个范畴等价 \(\mathsf{Fact}\simeq\mathsf{FinSet}^{{\cal P}}\)。

由于备注 3.7 中的函子 \(F_{E}\)，根据上述等价，我们有

\[\mathsf{FinSet}^{k}\to\mathsf{FinSet}^{{\cal P}}\]

**定义 3.9**：假设 \(E=[p_{1}...p_{k}]\) 和 \(\alpha:n\to k\)。我们定义布局 \(L_{(E,\alpha)}\) 如下：

1.  其形状元组是 \((p_{\alpha(1)},p_{\alpha(2)},...,p_{\alpha(n)})\)。
2.  其步长元组是 \((d_{1},d_{2},...,d_{n})\)，其中 \(d_{i}=\prod\limits_{j<\alpha(i)}p_{j}\)。

我们也令 \(f_{(E,\alpha)}\) 表示关联的布局函数。

假设 \(E=\left[23\right]\) 且 \(\varphi=\left(12\right)\in\Sigma_{2}\) 是非平凡对换。那么 \(L_{(E,\varphi)}=\left(3,2\right):\left(2,1\right)\)。

假设 \(E=\left[23\right],\varphi(1)=2,\varphi(2)=1,\varphi(3)=2\)。那么 \(L_{(E,\varphi)}=\left(3,2,3\right):\left(2,1,2\right)\)。这个布局看起来很奇怪，因为它的布局函数不是单射映射。然而，根据定义 2.2，它仍然是一个有效的布局。

假设 \(E=\left[222\right]\) 且 \(\varphi=\left(231\right)\in\Sigma_{3}\)，所以 \(\varphi\) 是一个阶为 \(3\) 的循环，其中 \(\varphi(1)=2\)，\(\varphi(2)=3\)，\(\varphi(3)=1\)。那么 \(L_{(E,\varphi)}=\left(2,2,2\right):\left(2,4,1\right)\)。

我们现在可以看到为什么 \(p_{i}\) 是素数。它用于为布局构造任何种类的步长元组，因为任何自然数都可以唯一地分解为素数的乘积。

**备注 3.11**：令 \(E=\left[p_{1}...p_{k}\right]\) 和 \(\alpha:n\to k\)。令 \(N=p_{1}\cdot p_{2}\cdot...\cdot p_{k}\) 且 \(N^{\alpha}=p_{\alpha(1)}\cdot p_{\alpha(2)}\cdot...\cdot p_{\alpha(n)}\)。在下面，考虑规范同构

\[\begin{array}{lll}[0,N)&\cong[0,p_{1})\times[0,p_{2})\times...\times[0,p_{k}) \\ [0,N^{\alpha})&\cong[0,p_{\alpha(1)})\times[0,p_{\alpha(2)})\times...\times[0,p _{\alpha(n)})\end{array}\]

那么关联的布局函数 \(f_{(E,\alpha)}:[0,N^{\alpha})\to[0,N)\subset\mathbb{N}\) 可以描述为多线性函数

\[[0,p_{\alpha(1)})\times[0,p_{\alpha(2)})\times...\times[0,p_{\alpha(n)})\to[0,p _{1})\times[0,p_{2})\times...\times[0,p_{k})\]

它将一个向量空间的基向量 \(\delta_{i}\) 发送到另一个向量空间的基向量 \(\beta_{\alpha(i)}\)，其中 \(i\in[1,n]\)。

特别地，如果 \(\alpha\) 本身是一个双射，那么 \(f_{(E,\alpha)}\) 限制为 \([0,N)\) 的一个自同构。

证明

我们注意到 \(f_{(E,\alpha)}:[0,N^{\alpha})\to[0,N)\subset\mathbb{N}\)。\(f_{(E,\alpha)}\) 的定义域是 \([0,N^{\alpha})\)，这很明显。然而，\(f_{(E,\alpha)}\) 的陪域是 \([0,N)\subset\mathbb{N}\) 不太明显。

\[f_{(E,\alpha)}=x_{\alpha_{(1)}}d_{1}+x_{\alpha_{(2)}}d_{2}+...+x_{\alpha_{(n)}} d_{n}\]

其中 \(x_{\alpha_{(i)}}\in[0,p_{\alpha(i)})\) 且 \(d_{i}=\prod\limits_{j<\alpha(i)}p_{j}\)。

所以我们有

$$\begin{aligned}
\max \left( f_{(E, \alpha)} \right) &= (p_{\alpha(1)} - 1) d_{1} + (p_{\alpha(2)} - 1) d_{2} + \ldots + (p_{\alpha(n)} - 1) d_{n} \\
&= (p_{\alpha(1)} - 1) \prod_{j < \alpha(1)} p_{j} + (p_{\alpha(2)} - 1) \prod_{j < \alpha(2)} p_{j} + \ldots + (p_{\alpha(n)} - 1) \prod_{j < \alpha(n)} p_{j} \\
\end{aligned}$$

不失一般性，我们假设 \(p_{\alpha(1)}\leq p_{\alpha(2)}\leq...\leq p_{\alpha(n)}\)。那么我们有

$$\begin{aligned}
{\max\left( f_{(E,\alpha)} \right)} & {= (p_{\alpha(1)} - 1)\prod\limits_{j < \alpha(1)}p_{j} + (p_{\alpha(2)} - 1)\prod\limits_{j < \alpha(2)}p_{j} + \ldots + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {\leq p_{\alpha(1)}\prod\limits_{j < \alpha(1)}p_{j} + (p_{\alpha(2)} - 1)\prod\limits_{j < \alpha(2)}p_{j} + \ldots + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {= \prod\limits_{j < \alpha(2)}p_{j} + (p_{\alpha(2)} - 1)\prod\limits_{j < \alpha(2)}p_{j} + \ldots + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {= p_{\alpha(2)}\prod\limits_{j < \alpha(2)}p_{j} + \ldots + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {\leq \prod\limits_{j < \alpha(3)}p_{j} + \ldots + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {\leq \ldots} \\
 & {\leq p_{\alpha(n)}\prod\limits_{j < \alpha(n - 1)}p_{j} + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {= \prod\limits_{j < \alpha(n)}p_{j} + (p_{\alpha(n)} - 1)\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {= p_{\alpha(n)}\prod\limits_{j < \alpha(n)}p_{j}} \\
 & {= \prod\limits_{j \leq \alpha(n)}p_{j}} \\
 & {\leq \prod\limits_{j \leq k}p_{j}} \\
 & {= N}
\end{aligned}$$

因此，我们有 $f_{(E,\alpha)}:\lbrack 0,N^{\alpha}) \rightarrow \lbrack 0,N) \subset \mathbb{N}$。
因为 𝑓(𝐸,𝛼) 是一个多线性函数，并且由于规范同构，𝑓(𝐸,𝛼) 可以描述为多线性函数

$$\begin{array}{r}
{\lbrack 0,p_{\alpha(1)}) \times \lbrack 0,p_{\alpha(2)}) \times \ldots \times \lbrack 0,p_{\alpha(n)})\overset{}{\rightarrow}\lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})}
\end{array}$$

我们记一个向量空间 $V = \lbrack 0,p_{\alpha(1)}) \times \lbrack 0,p_{\alpha(2)}) \times \ldots \times \lbrack 0,p_{\alpha(n)})$ 和一个向量空间  $W = \lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})$。那么布局函数 𝑓(𝐸,𝛼) 是一个线性映射 𝑉→𝑊。
假设 𝑣1, 𝑣2, 𝑎𝑣1, 𝑏𝑣2, 𝑎𝑣1 + 𝑏𝑣2 ∈𝑉，𝑓(𝐸,𝛼)(𝑣1) = 𝑤1，且 𝑓(𝐸,𝛼)(𝑣2) = 𝑤2。那么我们有

$$\begin{aligned}
{f_{(E,\alpha)}(v_{1})} & {= v_{1,1}d_{1} + v_{1,2}d_{2} + \ldots + v_{1,n}d_{n}} \\
{f_{(E,\alpha)}(v_{2})} & {= v_{2,1}d_{1} + v_{2,2}d_{2} + \ldots + v_{2,n}d_{n}} \\
{f_{(E,\alpha)}(av_{1})} & {= av_{1,1}d_{1} + av_{1,2}d_{2} + \ldots + av_{1,n}d_{n}} \\
 & {= af_{(E,\alpha)}(v_{1})} \\
{f_{(E,\alpha)}(bv_{2})} & {= bv_{2,1}d_{1} + bv_{2,2}d_{2} + \ldots + bv_{2,n}d_{n}} \\
 & {= bf_{(E,\alpha)}(v_{2})} \\
{f_{(E,\alpha)}(av_{1} + bv_{2})} & {= (av_{1} + bv_{2})_{1}d_{1} + (av_{1} + bv_{2})_{2}d_{2} + \ldots + (av_{1} + bv_{2})_{n}d_{n}} \\
 & {= af_{(E,\alpha)}(v_{1}) + bf_{(E,\alpha)}(v_{2})}
\end{aligned}$$

所以 𝑓(𝐸,𝛼) ∶𝑉→𝑊确实是一个线性（多线性）映射。
给定一个索引 1 ≤𝑖≤𝛼，令 𝛿𝑖∈ℕ×𝛼表示在所有位置为零，除了第 𝑘 个位置为 1 的坐标。注意这里的索引是 1 开始的，而不是命题 2.14 中使用的类似的 0 开始索引。对于 1 ≤𝑖≤𝛼，𝛿𝑖是向量空间 𝑉的基向量。
我们将 𝛿𝑖发送到 𝑓(𝐸,𝛼)，其中 1 ≤𝑖≤𝛼。那么我们有

$$\begin{aligned}
{f_{(E,\alpha)}(\delta_{i})} & {= d_{i}} \\
 & {= \prod\limits_{j < \alpha(i)}p_{j}}
\end{aligned}$$

给定规范同构 $\lbrack 0,N) \cong \lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})$，我们有多线性函数 $g:W\overset{}{\rightarrow}\mathbb{N}$

$$\begin{array}{r}
{g(w) = w_{1} + w_{2}p_{1} + w_{3}p_{1}p_{2} + \ldots + w_{k}\prod\limits_{j < k}p_{j}}
\end{array}$$

给定一个索引 1 ≤𝑖≤𝑘，令 𝛽𝑖∈ℕ×𝑘表示在所有位置为零，除了第 𝑘 个位置为 1 的坐标。对于 1 ≤𝑖≤𝑘，𝛽𝑖是向量空间 𝑊的基向量。
因此，我们有

$$\begin{matrix}
{f_{(E,\alpha)}(\delta_{i})} & {= \prod\limits_{j < \alpha(i)}p_{j}} \\
 & {= g(\beta_{\alpha(i)})}
\end{matrix}$$

这意味着向量空间 𝑉中的基向量 𝛿𝑖被多线性函数发送到向量空间 𝑊中的基向量 𝛽𝛼(𝑖)。
假设 𝑣= 𝑐1𝛿1 + 𝑐2𝛿2 + … + 𝑐𝛼𝛿𝛼∈𝑉。那么我们有

$$\begin{aligned}
{f_{(E,\alpha)}(v)} & {= f_{(E,\alpha)}(c_{1}\delta_{1} + c_{2}\delta_{2} + \ldots + c_{\alpha}\delta_{\alpha})} \\
 & {= (c_{1}\delta_{1} + c_{2}\delta_{2} + \ldots + c_{\alpha}\delta_{\alpha})_{1}d_{1} + (c_{1}\delta_{1} + c_{2}\delta_{2} + \ldots + c_{\alpha}\delta_{\alpha})_{2}d_{2} + \ldots + (c_{1}\delta_{1} + c_{2}\delta_{2} + \ldots + c_{\alpha}\delta_{\alpha})_{\alpha}d_{\alpha}} \\
 & {= c_{1}d_{1} + c_{2}d_{2} + \ldots + c_{\alpha}d_{\alpha}} \\
 & {= c_{1}f_{(E,\alpha)}(\delta_{1}) + c_{2}f_{(E,\alpha)}(\delta_{2}) + \ldots + c_{\alpha}f_{(E,\alpha)}(\delta_{\alpha})} \\
 & {= c_{1}g(\beta_{\alpha(1)}) + c_{2}g(\beta_{\alpha(2)}) + \ldots + c_{\alpha}g(\beta_{\alpha(\alpha)})} \\
 & {= g(c_{1}\beta_{\alpha(1)} + c_{2}\beta_{\alpha(2)} + \ldots + c_{\alpha}\beta_{\alpha(\alpha)})}
\end{aligned}$$

因此，我们为多线性函数 𝑓(𝐸,𝛼) ∶𝑉→𝑊建立了基向量映射。
给定 𝑣= 𝑐1𝛿1 + 𝑐2𝛿2 + … + 𝑐𝛼𝛿𝛼∈𝑉，它映射到 𝑤= 𝑐1𝛽𝛼(1) + 𝑐2𝛽𝛼(2) + … + 𝑐𝛼𝛽𝛼(𝛼) ∈𝑊。
证明完毕。

### 引理 3.12
详述备注 3.11，我们有以下引理，它表明范畴 Fact 中的复合与布局函数的复合是兼容的。
假设我们有有限集态射 $\alpha:\underset{―}{n} \rightarrow \underset{―}{k}$, $\beta:\underset{―}{m} \rightarrow \underset{―}{n}$，和一个表达式 $E = \lbrack p_{1}p_{2}\ldots p_{k}\rbrack$。写作 $\gamma = \alpha \circ \beta$。考虑 Fact 中的复合

$$\begin{array}{r}
{\gamma_{E}:E^{\gamma} = (E^{\alpha})^{\beta}\overset{\beta_{E^{\alpha}}}{\rightarrow}E^{\alpha}\overset{\alpha_{E}}{\rightarrow}E}
\end{array}$$

那么关联的布局函数满足复合等式

$$\begin{array}{r}
{f_{(E,\gamma)} = f_{(E,\alpha)} \circ f_{(E^{\alpha},\beta)}}
\end{array}$$


证明
令 𝑁= 𝑝1 ⋅𝑝2 ⋅… ⋅𝑝𝑘， 𝑁𝛼= 𝑝𝛼(1) ⋅𝑝𝛼(2) ⋅… ⋅𝑝𝛼(𝑛)，且 𝑁𝛾= 𝑝𝛾(1) ⋅𝑝𝛾(2) ⋅… ⋅𝑝𝛾(𝑚)。我们使用
规范同构

$$\begin{matrix}
{\lbrack 0,N)} & {\cong \lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})} \\
{\lbrack 0,N^{\alpha})} & {\cong \lbrack 0,p_{\alpha(1)}) \times \lbrack 0,p_{\alpha(2)}) \times \ldots \times \lbrack 0,p_{\alpha(n)})} \\
{\lbrack 0,N^{\gamma})} & {\cong \lbrack 0,p_{\gamma(1)}) \times \lbrack 0,p_{\gamma(2)}) \times \ldots \times \lbrack 0,p_{\gamma(m)})}
\end{matrix}$$

来写出相关布局函数的定义域和陪域。
更具体地说，我们有

$$\begin{matrix}
{f_{(E,\alpha)}:\lbrack 0,N^{\alpha})} & {\rightarrow \lbrack 0,N)} \\
{f_{(E^{\alpha},\beta)}:\lbrack 0,N^{\gamma})} & {\rightarrow \lbrack 0,N^{\alpha})} \\
{f_{(E,\gamma)}:\lbrack 0,N^{\gamma})} & {\rightarrow \lbrack 0,N)}
\end{matrix}$$

我们试图等价多线性函数

$$\begin{array}{r}
{f_{(E,\gamma)}:\lbrack 0,p_{\gamma(1)}) \times \lbrack 0,p_{\gamma(2)}) \times \ldots \times \lbrack 0,p_{\gamma(m)})\overset{}{\rightarrow}\lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})}
\end{array}$$


与两个多线性函数的复合

$$\begin{matrix}
f_{(E,\alpha)} & {:\lbrack 0,p_{\alpha(1)}) \times \lbrack 0,p_{\alpha(2)}) \times \ldots \times \lbrack 0,p_{\alpha(n)})\overset{}{\rightarrow}\lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})} \\
f_{(E^{\alpha},\beta)} & {:\lbrack 0,p_{\gamma(1)}) \times \lbrack 0,p_{\gamma(2)}) \times \ldots \times \lbrack 0,p_{\gamma(m)})\overset{}{\rightarrow}\lbrack 0,p_{\alpha(1)}) \times \lbrack 0,p_{\alpha(2)}) \times \ldots \times \lbrack 0,p_{\alpha(n)})}
\end{matrix}$$

我们记一个向量空间 $V = \lbrack 0,p_{\alpha(1)}) \times \lbrack 0,p_{\alpha(2)}) \times \ldots \times \lbrack 0,p_{\alpha(n)})$，一个向量空间 $W = \lbrack 0,p_{1}) \times \lbrack 0,p_{2}) \times \ldots \times \lbrack 0,p_{k})$,，和一个向量空间 $U = \lbrack 0,p_{\gamma(1)}) \times \lbrack 0,p_{\gamma(2)}) \times \ldots \times \lbrack 0,p_{\gamma(m)})$。𝑉, 𝑊, 和 𝑈的基
向量分别为 𝛿𝑖, 𝜎𝑗, 和 𝜏𝑙，其中 1 ≤𝑖≤𝑛， 1 ≤𝑗≤𝑘，和 1 ≤𝑙≤𝑚。
基于备注 3.11 的基向量映射，给定 𝑢= 𝑐1𝜏1 + 𝑐2𝜏2 + … + 𝑐𝑚𝜏𝑚∈𝑈，
通过 𝑓(𝐸𝛼,𝛽)，它映射到 𝑣= 𝑐1𝛿𝛽(1) + 𝑐2𝛿𝛽(2) + … + 𝑐𝑚𝛿𝛽(𝑚) ∈𝑉。然后通过 𝑓(𝐸,𝛼)，它映射到
𝑤= 𝑐1𝜎𝛼(𝛽(1)) + 𝑐2𝜎𝛼(𝛽(2)) + … + 𝑐𝑚𝜎𝛼(𝛽(𝑚)) ∈𝑊。
给定 𝑢= 𝑐1𝜏1 + 𝑐2𝜏2 + … + 𝑐𝑚𝜏𝑚∈𝑈，通过 𝑓(𝐸,𝛾)，因为 𝛾= 𝛼∘𝛽， 𝛾(𝑖) = 𝛼(𝛽(𝑖))，它映射
到 𝑤′ = 𝑐1𝜎𝛾(1) + 𝑐2𝜎𝛾(2) + … + 𝑐𝑚𝜎𝛾(𝑚) = 𝑐1𝜎𝛼(𝛽(1)) + 𝑐2𝜎𝛼(𝛽(2)) + … + 𝑐𝑚𝜎𝛼(𝛽(𝑚)) ∈𝑊。
因为 𝑤= 𝑤′，我们有 𝑓(𝐸,𝛾) = 𝑓(𝐸,𝛼) ∘𝑓(𝐸𝛼,𝛽)。
证明完毕。
在引理 3.12 中，复合的按模式可接纳性条件（定义 2.12）被满足。要看到这一点，我们有

$$\begin{matrix}
E & {= \lbrack p_{1}p_{2}\ldots p_{k}\rbrack} \\
E^{\alpha} & {= \lbrack p_{\alpha(1)}p_{\alpha(2)}\ldots p_{\alpha(n)}\rbrack} \\
E^{\gamma} & {= \lbrack p_{\gamma(1)}p_{\gamma(2)}\ldots p_{\gamma(m)}\rbrack}
\end{matrix}$$

$$\begin{matrix}
L_{(E,\alpha)} & {= \left( p_{\alpha(1)},p_{\alpha(2)},\ldots,p_{\alpha(n)} \right):\left( d_{1},d_{2},\ldots,d_{n} \right)}
\end{matrix}$$

其中 $d_{i} = \prod\limits_{j < \alpha(i)}p_{j}$。

\[L_{(E^{\alpha},\beta)}\ =\left(p_{\alpha(\beta(1))},p_{\alpha(\beta(2))},..., p_{\alpha(\beta(m))}\right):(d_{1}^{\prime},d_{2}^{\prime},...,d_{m}^{\prime})\]

其中 \(d_{i}^{\prime}=\prod\limits_{j<\beta(i)}p_{\alpha(j)}\)。 因为

\[M =p_{\alpha(1)}\cdot p_{\alpha(2)}\cdot...\cdot p_{\alpha(n)}\] \[=\left(\prod\limits_{j<\beta(i)}p_{\alpha(j)}\right)\cdot p_{ \alpha(\beta(i))}\cdot p_{\alpha(\beta(i)+1)}\cdot...\cdot p_{\alpha(n)}\] \[=\left(\prod\limits_{j<\beta(i)}p_{\alpha(j)}\right)\cdot M^{\prime}\]

因此，\(M\) 被 \(d_{i}^{\prime}\) 左可除，\(M^{\prime}\) 被 \(p_{\alpha(\beta(i))}\) 弱左可除并且也左可除，并且复合的按模式可接纳性条件被满足。

定义 2.17 中的不相交条件当 \(\beta:m\to n\) 是单射函数时被满足，当不是单射时可能被违反。

当 \(\beta:m\to n\) 是单射时，我们有 \(m\leq n\)，且对于 \(i\neq j\)，有 \(\beta(i)\neq\beta(j)\)。根据定义 2.16，对于每个模式 \(i\in[1,m]\)，我们有 \(N_{i}=p_{\alpha(\beta(i))}\)，\(I_{i}=[d_{i}^{\prime},d_{i}^{\prime}(N_{i}-1)]\)。 \(M^{\prime}=p_{\alpha(1)}\cdot p_{\alpha(2)}\cdot...\cdot p_{\alpha(n-1)}\)。所以定义区间是 \(J_{i}=I_{i}\cap[1,M^{\prime})\)。因为 \(d_{i}^{\prime}=\prod\limits_{j<\beta(i)}p_{\alpha(j)}\geq 1\)，\(d_{i}^{\prime}(N_{i}-1)=\prod\limits_{j<\beta(i)}p_{\alpha(j)}\cdot(p_{\alpha( \beta(i))}-1)=\prod\limits_{j\leq\beta(i)}p_{\alpha(j)}-\prod\limits_{j<\beta(i )}p_{\alpha(j)}<M^{\prime}\)。因此，\(J_{i}=I_{i}=[\prod\limits_{j<\beta(i)}p_{\alpha(j)},\prod\limits_{j\leq\beta (i)}p_{\alpha(j)}-\prod\limits_{j<\beta(i)}p_{\alpha(j)}]\)。假设我们有一个不同的模式 \(k\)，\(k\neq i\)。那么 \(J_{k}=I_{k}=[\prod\limits_{j<\beta(k)}p_{\alpha(j)},\prod\limits_{j\leq\beta (k)}p_{\alpha(j)}-\prod\limits_{j<\beta(k)}p_{\alpha(j)}]\)。因为 \(\beta(i)\neq\beta(k)\)，不失一般性，我们假设 \(\beta(i)<\beta(k)\)。那么我们有

\[\prod\limits_{j<\beta(k)}p_{\alpha(j)}-\left(\prod\limits_{j\leq\beta(i)}p_{ \alpha(j)}-\prod\limits_{j<\beta(i)}p_{\alpha(j)}\right)\ =\ \prod\limits_{j <\beta(k)}p_{\alpha(j)}-\prod\limits_{j\leq\beta(i)}p_{\alpha(j)}+\prod\limits_ {j<\beta(i)}p_{\alpha(j)}\]

因此，对于任何 \(i\neq k\)，\(J_{i}\cap J_{k}=\emptyset\)。不相交条件被满足。

当 \(\beta:m\to n\) 不是单射时，我们没有对于 \(i\neq j\)，\(\beta(i)\neq\beta(j)\)。不相交条件可能被违反。

所以引理 3.12 实际上证明了定理 2.18 对于第二布局具有任意步长（尚未具有任意形状）且满足定义 2.17 的布局。

**定义 3.14**：我们现在定义一个从范畴 **Fact** 到范畴 **FinSet** 的“实现”函子，它将有序因式分解的态射发送到它们关联的布局函数。

令 \(R:\textbf{Fact}\rightarrow\textbf{FinSet}\) 为定义如下的函子：

1. 令 \(E=[p_{1}p_{2}...p_{k}]\) 为 **Fact** 的一个对象，令 \(N=p_{1}\cdot p_{2}\cdot...\cdot p_{k}\)。那么 \(R(E)=[0,N)\)。
2. 对于每个态射 \(\alpha_{E}\,:\,E^{\alpha}\,\to\,E\)，令 \(R(\alpha_{E})\,=\,f_{(E,\alpha)}\,:\,[0,N^{\alpha})\,\to\,[0,N)\) 如定义 3.9 中所述。

根据引理 3.12，\(R:{\bf Fact}\to{\bf FinSet}\) 确实定义了一个函子，因为它尊重态射的复合和单位元。

我们注意到，如前所述，\(R\) 的像并不包含每个可能可表示为布局函数的函数。然而，它确实包含每个可表示为布局函数的 \([0,N)\,\stackrel{{\cong}}{{\to}}\,[0,N)\) 的自同构。

**命题 3.15**：令 \(N>0\) 为一个正整数，令 \(f:[0,N)\,\to\,[0,N)\) 为一个自同构，使得存在一个大小为 \(N\) 的布局 \(L\) 满足 \(f=f_{L}\)。那么 \(f_{L}\) 在实现函子 \(R\) 的像中。

证明

不失一般性，我们可以假设 \(L\) 的形状元组由 \((p_{1},p_{2},...,p_{k})\) 给出，其中 \(p_{i}\) 都是素数且 \(N=p_{1}\cdot p_{2}\cdot...\cdot p_{k}\)。

为了使 \(f_{L}\) 成为 \([0,N)\) 的自同构，排序后的 \(L\)，\(L^{\varphi}\)，必须具有以下形式

\[L^{\varphi}=\left(p_{\varphi(1)},p_{\varphi(2)},...,p_{\varphi(k)}\right):\, \left(1,p_{\varphi(1)},p_{\varphi(1)}p_{\varphi(2)},...,\,\prod_{1\leq i<k}p_{ \varphi(i)}\right)\]

对于某个排列 \(\varphi\,\in\,\Sigma_{k}\)。这意味着如果我们令 \(\psi\,=\,\varphi^{-1}\) 为逆排列，那么

\[\psi_{E}:E^{\psi}=[p_{1}p_{2}...p_{k}]=[p_{\psi(\varphi(1))}p_{\psi(\varphi(2)) }...p_{\psi(\varphi(k))}]\to E=[p_{\varphi(1)}p_{\varphi(2)}...p_{\varphi(k)}]\]

是 **Fact** 中的一个态射，且 \(R(\psi_{E})=f_{L}\)。

证明完毕。

**备注 3.16**：解释命题 3.15 的一种方式是，如果我们取 **Fact** 内部的最大群胚 \({\bf Fact}^{\cong}\)，即所有可逆态射的子范畴，那么

\[R:{\bf Fact}^{\cong}\to{\bf FinSet}\]

恰好刻画了那些可表示为布局的排列。我们这种描述的动机是，对于一个固定的整数 \(N\,>\,0\)，\(\Sigma_{N}\) 的子集 \(\Sigma^{L}_{N}\)（那些可表示为布局函数的自同构）通常不是一个子群（因为通常不封闭于群乘法，即复合）。

相反，如果我们令

\[{\bf Fact}^{\cong}_{N}\,\subset\,{\bf Fact}^{\cong}\]

为那些对象 \([p_{1}p_{2}...p_{k}]\) 的满子群胚，其中 \(N=p_{1}\cdot p_{2}\cdot...\cdot p_{k}\)，那么 \(\Sigma^{L}_{N}\) 由 \(R\) 在 \({\bf Fact}^{\cong}_{N}\) 上的像中的那些态射组成。然而，我们看到 \(\Sigma^{L}_{N}\) 在取群逆的操作下是封闭的（取排列到其逆的对象也在 \(\textsf{Fact}_{N}^{\infty}\) 中）。此外，在 \(N\) 是素数幂 \(p^{k}\) 的特殊情况下，\(\Sigma_{N}^{L}\) 实际上是一个子群，并且同构于对称群 \(\Sigma_{k}\)。这对应于 \(\textsf{Fact}_{p^{k}}^{\infty}\) 是一个具有单个对象 \([pp\dots p]\) 的群胚，即一个群。
